# Revue adversariale du delta A+B+C (2026-07-30) — confirmees

## R1. [major/regressions] R/table_continuous_lm.R
Binary categorical `by` hard-errors with a misleading 'internal invariant / report a bug' message under global contr.sum options; the nocov 'unreachable' claim is false
CLAIM: The new by-name coefficient lookup in fit_categorical_predictor_lm_rows() (`coef_idx <- match(paste0("x", levs[i + 1L]), names(cf))`, ~line 2062) assumes the fit uses treatment coding, and its failure arm is annotated '# nocov start: defensive ... the fit always names its dummies x<level> under treatment coding'. That assumption only holds under R's default global contrasts: lm() names factor dummies from options(contrasts=), and afex-style workflows (options(contrasts = c("contr.sum", "contr.po
EVIDENCE: Repro (R 4.6.1, current HEAD 526b03b): options(contrasts = c("contr.sum", "contr.poly")); d <- data.frame(y = c(rnorm(10, 10), rnorm(10, 12)), g = factor(rep(c("a","b"), each = 10))); table_continuous_lm(d, select = y, by = g, output = "long") -> Error class spicy_internal_invariant: "Internal invariant failed: coefficient `xb` not found in the fitted model. i Please report this at https://github.com/amaltawfik/spicy/issues." Same call with 3 levels returns correct emmeans (a/b/c match tapply me

## R2. [minor/regressions] R/table_categorical.R
Crash regression: factor with a declared-but-unobserved "(Missing)" level plus NA now dies with raw 'factor level [4] is duplicated'
CLAIM: The new .add_missing_level() helper (R/table_categorical.R:46-54) appends the missing label via factor(f, levels = c(levels(f), missing_label)). The collision guard that renames missing_label (lines 1068-1083) scans only observed values (as.character(data[[nm]]) drops unobserved factor levels), so when a selected variable or the by variable declares "(Missing)" among its levels without observing it and the column contains NA (default drop_na = FALSE), the levels vector gets a duplicate and facto
EVIDENCE: Repro (current HEAD 526b03b): table_categorical(data.frame(x = factor(c("A","B",NA), levels = c("A","B","(Missing)"))), select = x) -> Error: 'factor level [4] is duplicated' (raw simpleError, no spicy_ class). Under a83e6c9 the equivalent code path did x <- as.character(x); x[is.na(x)] <- "(Missing)" with missing_label unchanged (guard checks all_values = observed values only, and as.character(factor) yields observed values), producing a valid table with rows A=1, B=1, (Missing)=1. If "(Missing

## R3. [major/completude] R/table_categorical.R
Regression: raw crash when a factor declares an unobserved "(Missing)" level and has real NAs
CLAIM: The new .add_missing_level() (R/table_categorical.R:46-54, introduced by lot B) appends the missing_label level without checking declared levels, and the collision guard at lines 1069-1083 only scans observed values (as.character(data[[nm]])), never levels(). A selected column (or by) that DECLARES a level literally named "(Missing)" with zero observations, combined with actual NAs under the default drop_na = FALSE, now crashes the entire multi-variable table with an unclassed base error, where 
EVIDENCE: table_categorical(data.frame(xm = factor(c('A','B','A',NA), levels = c('A','B','(Missing)'))), select = xm) -> Error 'factor level [4] is duplicated', class simpleError (not spicy_*). Same crash with by. Witnesses: the observed-value case factor(c('A','(Missing)','A',NA)) is correctly guarded (renders '(Missing_1)'), and drop_na = TRUE works. The pre-fix code path (x <- as.character(x); x[is.na(x)] <- '(Missing)') tabulates the same input as A | B | (Missing). The '(Missing)' label is forcats' d

## R4. [minor/completude] R/table_continuous_lm.R
Finding 32 fix incomplete: per-outcome single observed group still yields a silent all-NA row plus a spurious "M (NA)" print column
CLAIM: The new single-level-by guard (R/table_continuous_lm.R:1194-1207) only inspects the full by_vector before fitting. When per-outcome complete-case filtering (NA in y, weights, or covariates) leaves fewer than two observed groups for one outcome, make_empty_lm_rows (called at lines 1754/1882) still emits the all-NA row with no classed warning and no note - exactly the silent degradation finding 32(1) documented - and the ASCII print grows an empty "M (NA)" column that pollutes the header for ALL o
EVIDENCE: d7 <- data.frame(y1 = 1:5, y2 = c(1,2,3,NA,NA), g = factor(c('a','a','a','b','b'))); table_continuous_lm(d7, select = c(y1, y2), by = g, output = 'long') emits ZERO warnings and returns a y2 row with level=NA, emmean=NA, n=NA; print() renders header 'M (a) M (b) ... M (NA)' with an entirely empty y2 line and an empty trailing column on y1's line. Contrast: the global case (g with 1 observed level) correctly aborts with spicy_invalid_data, and the saturated case correctly warns spicy_undefined_st

## R5. [minor/completude] R/table_categorical.R
New doc overclaims that rounded display cells and their Total agree
CLAIM: The new @details text (R/table_categorical.R:531-535, mirrored in the fmt_n comment at 1173-1178) states counts are rounded 'in cells and margins alike -- the SPSS Crosstabs convention -- so displayed cells and their Total agree'. Cells and margins are rounded independently, so agreement is not guaranteed: two half-count cells can each display 0 while their Total displays 1.
EVIDENCE: d <- data.frame(x = factor(c('A','A','B')), g = factor(c('g1','g1','g2')[c(1,3,1)]), w = c(0.5, 0.5, 2)); table_categorical(d, select = x, by = g, weights = w) displays row A as g1 n=0, g2 n=0, Total n=1 (exact values 0.5 + 0.5 = 1.0; round-half-to-even sends both cells to 0, the margin to 1). The machine outputs are exact (0.5/0.5/1.0), so only the documented display-consistency promise is false. The new pinning test only covers a case (3.75 -> 4 in both cell and margin) where the identity happ

## R6. [minor/completude] R/table_continuous_lm.R
Labelled-by doc claims order parity with the sibling tables that does not hold
CLAIM: The new @param by bullet (R/table_continuous_lm.R:54-59) says a labelled by is treated as categorical 'in ascending code order -- the same grouping [table_continuous()] and [table_categorical()] apply to the same column', but the lot-C order unification made those siblings use order of FIRST APPEARANCE for non-factor by (R/table_continuous.R:851-858 and the g_fac construction in table_categorical). table_continuous_lm's factor(by_codes) sorts ascending, so the same labelled column produces diffe
EVIDENCE: dl$gl <- haven::labelled(rep(c(2,1,3), each = 4), labels = c(Low=1, Mid=2, High=3)): table_continuous(dl, select = y, by = gl) groups '2 | 1 | 3' and table_categorical columns '2 n | 1 n | 3 n' (appearance), while table_continuous_lm(dl, select = y, by = gl) levels '1 | 2 | 3' (ascending). Group membership and all statistics are correct; only the cross-referenced ordering claim in the new documentation (and the commit's 'conventions unified family-wide') is inaccurate.

## R7. [major/coherence] R/table_categorical.R
Declared-but-unobserved by level 'Total': margin displayed as the user's group (lot B x lot C interaction)
CLAIM: margin_key is derived from DECLARED group levels (l.1883-1894) while cross_tab() renames its margin only on an OBSERVED collision. When a by factor declares a level 'Total' that never occurs, table_categorical() believes the margin is 'Total_1', but ct_n's last column is literally named 'Total'; the group-read at l.2081-2093 matches it by name before the lot-C zero-fill fallback can fire. With include_total=TRUE the margin appears twice (as group 'Total' and as 'Total_1') plus a spurious spicy_r
EVIDENCE: d <- data.frame(x=factor(c('A','B','A','B','A')), g=factor(c('g1','g1','g2','g2','g1'), levels=c('g1','g2','Total'))); table_categorical(d, select=x, by=g, output='long') emits warning "`g` has a group level literally named \"Total\"..." and returns group 'Total' n=3/2 (the margin, oracle 0/0) AND group 'Total_1' n=3/2; wide output has both 'Total n'=3 and 'Total_1 n'=3. With include_total=FALSE: groups g1/g2/'Total' where 'Total' carries n=3/2 margin counts instead of zeros. Script scratchpad/t

## R8. [major/coherence] R/table_categorical.R
Raw 'factor level [4] is duplicated' crash when a factor declares an unobserved '(Missing)' level and has NAs (default drop_na = FALSE)
CLAIM: .add_missing_level() (l.46-53) appends missing_label to levels(f) unconditionally, and the missing_label uniqueness guard (l.1068-1083) scans only OBSERVED values (as.character(data[[nm]])), never declared factor levels. A factor with a declared-but-unobserved '(Missing)' level plus at least one NA therefore produces factor(levels = c(..., '(Missing)', '(Missing)')) and base R aborts with an uncaught, unclassed 'factor level [4] is duplicated' that kills the entire table. Reproduces in the selec
EVIDENCE: table_categorical(data.frame(x=factor(c('a','b',NA), levels=c('a','b','(Missing)'))), select=x, output='long') -> ERROR 'factor level [4] is duplicated'. Same crash with by (t1_interactions.R T2/T2b) and with the NA on the by side: g=factor(c('u','v',NA,'u'), levels=c('u','v','(Missing)')) (t3_batch.R T13/T14). Fix direction: include declared levels in the all_values scan at l.1068-1071, or make .add_missing_level() reuse an existing missing_label level.

## R9. [minor/coherence] R/table_continuous.R
New non-finite effect-size guard misses NaN: NaN Hedges' g reaches output='long' without the promised warning
CLAIM: The lot-C guard at l.979, `if (!is.na(es_row$es_value) && !is.finite(es_row$es_value))`, short-circuits on NaN because is.na(NaN) is TRUE. When both groups are constant with EQUAL means (pooled SD 0, mean difference 0 -> 0/0), es_value = NaN and es_ci_lower/upper = NaN pass through un-blanked into the long/data.frame outputs, with no spicy_undefined_stat warning for the effect size — contradicting the fix's stated contract ('becomes an NA cell ... never a printed Inf') and leaving the first grou
EVIDENCE: d <- data.frame(cst=rep(5,8), g=factor(rep(c('m','f'), each=4))); table_continuous(d, select=cst, by=g, effect_size='hedges_g', p_value=FALSE, output='long') -> es_value = c(NaN, NA), es_ci_lower/upper = NaN, and the only spicy warning emitted concerns the t-test, none for the ES. is.nan(r$es_value[1]) is TRUE. Script scratchpad/t2_more.R (T4). Fix: test !is.finite() alone (it already covers NA) or add is.nan() explicitly.

## R10. [minor/coherence] R/table_continuous_lm.R
Branch marked 'nocov: defensive/unreachable' is reachable via global options(contrasts=): whole table aborts as 'internal bug'
CLAIM: The new by-name coefficient lookup for the displayed difference (l.2056-2075) aborts with spicy_internal_invariant ('this is a bug in spicy: please report it at ...') when match(paste0('x', level), names(cf)) fails, and the comment plus nocov marker claim this cannot happen because x is an unordered factor. The claim is false: coefficient naming depends on the GLOBAL contrasts option, not on the ordered class. Under options(contrasts=c('contr.sum','contr.poly')) — a standard setting for ANOVA us
EVIDENCE: old <- options(contrasts=c('contr.sum','contr.poly')); table_continuous_lm(data.frame(y=1:6, g=factor(rep(c('a','b'), each=3))), select=y, by=g, output='long') -> Error (spicy_internal_invariant): "Internal invariant failed: coefficient `xb` not found in the fitted model. Please report this at https://github.com/amaltawfik/spicy/issues." By contrast a per-factor contrasts() attribute works fine (emmeans 11.5/21.5, estimate 10 — scratchpad/t3_batch.R T16). Script scratchpad/t2_more.R (T6). Fix: p

## R11. [minor/coherence] R/table_categorical.R
New doc/comment overclaims that integer-rounded weighted cells always sum to the displayed Total
CLAIM: The new roxygen block (l.531-537) and the fmt_n comment (l.1173-1178) assert that rounding cells and margins alike means 'displayed cells and their Total agree'. Independent half-even rounding does not guarantee additivity: cells whose exact weighted counts are 0.5 each display as 0 while their exact Total 1.0 displays as 1. The displayed row 0 + 0 != 1, and a displayed n=0 cell still carries a non-zero percent (33.3 / 100.0) — the very display incoherence audit finding 5 complained about, now d
EVIDENCE: d <- data.frame(x=factor(c('A','A','B')), g=factor(c('g1','g2','g1')), w=c(0.5,0.5,1)); table_categorical(d, select=x, by=g, weights=w) displays row A: 'g1 n'=0 (g1 %=33.3), 'g2 n'=0 (g2 %=100.0), 'Total n'=1 — cells sum to 0, Total shows 1. Script scratchpad/t2_more.R (T5). SPSS also rounds independently, so the convention is defensible; the sentence 'so displayed cells and their Total agree' is the defect and should be softened.

## R12. [minor/coherence] vignettes/association-measures.Rmd
Tau-c relabel to "Stuart's Tau-c" incomplete: vignette still teaches the label that no longer appears in any output
CLAIM: The delta renames the displayed measure label from "Kendall's Tau-c" to "Stuart's Tau-c" in assoc.R, cross_tab.R, the parse regex, tests and snapshot, but vignettes/association-measures.Rmd (l.44 dispatch table and l.147 section heading) still uses "Kendall's Tau-c". A reader following the vignette will look for a label that assoc_measures()/cross_tab()/table_categorical() no longer print, and the vignette's dispatch table no longer matches the assoc_measures() output column it documents.
EVIDENCE: grep: vignettes/association-measures.Rmd:44 '| Ordinal x Ordinal (rectangular) | Kendall's Tau-c | `kendall_tau_c()` |' and :147 '### Kendall's Tau-c', while R/assoc.R:1890 now emits measure = "Stuart's Tau-c" and tests/_snaps/snapshots.md pins the new label. The Rd title was updated to the dual form ('Kendall's Tau-c (Stuart's Tau-c)'); the vignette was not.

## Refutees
