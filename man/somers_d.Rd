% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assoc.R
\name{somers_d}
\alias{somers_d}
\title{Somers' D}
\usage{
somers_d(
  x,
  direction = c("row", "column", "symmetric"),
  detail = FALSE,
  conf_level = 0.95,
  digits = 3L
)
}
\arguments{
\item{x}{A contingency table (of class \code{table}).}

\item{direction}{Direction of prediction:
\code{"row"} (default, column predicts row),
\code{"column"} (row predicts column),
or \code{"symmetric"} (harmonic mean of both directions).}

\item{detail}{Logical. If \code{FALSE} (default), return the estimate
as a numeric scalar. If \code{TRUE}, return a named numeric vector
including confidence interval and p-value.}

\item{conf_level}{A single number strictly between 0 and 1 giving
the confidence level (default \code{0.95}). Only used when
\code{detail = TRUE}. Set to \code{NULL} to omit the confidence interval.
Any other value -- including percentages such as \code{95} -- raises
a classed error (\code{spicy_invalid_input}).}

\item{digits}{Number of decimal places used when printing the
result (default \code{3}). Only affects the \code{detail = TRUE} output.}
}
\value{
Same structure as \code{\link[=cramer_v]{cramer_v()}}: a scalar when
\code{detail = FALSE}, a named vector when \code{detail = TRUE}.
The p-value tests H0: D = 0 (Wald z-test).
}
\description{
\code{somers_d()} computes Somers' D for a two-way contingency
table of ordinal variables.
}
\details{
Somers' D is an asymmetric ordinal measure defined as
\eqn{d = (C - D) / (C + D + T)}, where \eqn{T} is the
number of pairs tied on the independent variable. The
symmetric version (\code{direction = "symmetric"}) is the
\emph{harmonic} mean of the two asymmetric values, matching the
SPSS / PSPP convention; this is \strong{not} identical to
Kendall's Tau-b (which is the \emph{geometric} mean of the same
two quantities), although the two often agree to two
decimals. It is computed via the equivalent closed form
\eqn{2(C - D)} divided by the sum of the two asymmetric
denominators, so a table with exactly as many concordant as
discordant pairs (e.g. an independence pattern) yields the
well-defined value 0 -- the harmonic-mean form is 0/0 there --
as printed by SPSS / PSPP. The symmetric estimate
is \code{NA} only when one of the asymmetric directions is itself
undefined (with the same \code{spicy_undefined_stat} warning). No
analytic SE / CI is reported for the symmetric form: its \code{se}
is always \code{NA}, matching PSPP, which prints no ASE for it
(DescTools offers no symmetric form at all).

The default \code{direction = "row"} differs deliberately from
\code{\link[=lambda_gk]{lambda_gk()}} and \code{\link[=uncertainty_coef]{uncertainty_coef()}} (which default to
\code{"symmetric"}): Somers' D is intrinsically asymmetric, its
symmetric form is a derived convention without inference, and
the reference implementation (\code{DescTools::SomersDelta()})
offers only the two asymmetric directions, defaulting to
\code{"row"}.

Standard error formulas for the asymmetric directions follow
the DescTools implementations (Signorell et al., 2024); see
\code{\link[=cramer_v]{cramer_v()}} for full references.
}
\examples{
tab <- table(sochealth$education, sochealth$self_rated_health)
somers_d(tab, direction = "row")
somers_d(tab, direction = "column", detail = TRUE)

}
\references{
Somers, R. H. (1962). A new asymmetric measure of association
for ordinal variables. \emph{American Sociological Review}, 27(6),
799-811. \doi{10.2307/2090408}

Brown, M. B., & Benedetti, J. K. (1977). Sampling behavior of
tests for correlation in two-way contingency tables. \emph{Journal of
the American Statistical Association}, 72(358), 309-315.
\doi{10.1080/01621459.1977.10480995}
}
\seealso{
\code{\link[=kendall_tau_b]{kendall_tau_b()}}, \code{\link[=gamma_gk]{gamma_gk()}}, \code{\link[=assoc_measures]{assoc_measures()}}

Other association measures:
\code{\link[=assoc_measures]{assoc_measures()}},
\code{\link[=contingency_coef]{contingency_coef()}},
\code{\link[=cramer_v]{cramer_v()}},
\code{\link[=gamma_gk]{gamma_gk()}},
\code{\link[=goodman_kruskal_tau]{goodman_kruskal_tau()}},
\code{\link[=kendall_tau_b]{kendall_tau_b()}},
\code{\link[=kendall_tau_c]{kendall_tau_c()}},
\code{\link[=lambda_gk]{lambda_gk()}},
\code{\link[=phi]{phi()}},
\code{\link[=uncertainty_coef]{uncertainty_coef()}},
\code{\link[=yule_q]{yule_q()}}
}
\concept{association measures}
