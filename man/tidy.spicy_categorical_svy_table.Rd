% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_categorical_svy_print.R
\name{tidy.spicy_categorical_svy_table}
\alias{tidy.spicy_categorical_svy_table}
\alias{glance.spicy_categorical_svy_table}
\title{Tidying methods for a \code{spicy_categorical_svy_table}}
\usage{
\method{tidy}{spicy_categorical_svy_table}(x, ...)

\method{glance}{spicy_categorical_svy_table}(x, ...)
}
\arguments{
\item{x}{A \code{spicy_categorical_svy_table} returned by
\code{\link[=table_categorical_svy]{table_categorical_svy()}}.}

\item{...}{Ignored, for S3 compatibility.}
}
\value{
A \code{tbl_df} (or \code{data.frame} when tibble is not installed).
}
\description{
Standard \code{\link[broom:tidy]{broom::tidy()}} and \code{\link[broom:glance]{broom::glance()}} interfaces for an
object returned by \code{\link[=table_categorical_svy]{table_categorical_svy()}}.
}
\details{
\code{tidy()} returns one row per (variable x level x column block), which
is the LONG reading of a table whose blocks are columns. Columns:
\code{variable}, \code{label}, \code{level}, \code{group} (the \code{by} level, or the margin,
\code{NA} without \code{by}), \code{total} (whether that block is the margin),
\code{n} (observed), \code{estimate} (the estimated percentage), \code{conf.low},
\code{conf.high}, \code{deff}. The header rows carry no level statistic and do
not appear; their p-value is what \code{glance()} is for.

\code{glance()} returns one row per variable: \code{variable}, \code{label},
\code{n_levels}, \code{p.value}, \code{statistic_type} (the \code{svychisq()} statistic
asked for), \code{degf} (the design's own), \code{nobs}, \code{weighted.nobs}.

\code{n_levels} counts the levels the TABLE displays, so a \code{(Missing)}
display level counts; the test behind \code{p.value} runs on the complete
cases and the observed levels only, as it does in
\code{\link[=table_categorical]{table_categorical()}}.
}
\keyword{internal}
