% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_apa.R
\name{table_apa}
\alias{table_apa}
\title{Build APA-Style Cross-Tabulation Tables From Multiple Row Variables}
\usage{
table_apa(
  data,
  row_vars,
  group_var,
  labels = NULL,
  levels_keep = NULL,
  include_total = TRUE,
  drop_na = TRUE,
  weights = NULL,
  rescale = FALSE,
  correct = FALSE,
  simulate_p = TRUE,
  simulate_B = 2000,
  percent_digits = 1,
  p_digits = 3,
  v_digits = 2,
  decimal_mark = ".",
  output = c("wide", "long", "tinytable", "flextable", "excel", "clipboard", "word"),
  style = c("auto", "raw", "report"),
  indent_text = "  ",
  indent_text_excel_clipboard = "      ",
  add_multilevel_header = TRUE,
  blank_na_wide = FALSE,
  excel_path = NULL,
  excel_sheet = "APA",
  clipboard_delim = "\\t",
  word_path = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{row_vars}{Character vector of variable names to place in rows.}

\item{group_var}{Single character variable name used for columns/groups.}

\item{labels}{Optional character labels for \code{row_vars} (same length).}

\item{levels_keep}{Optional character vector of levels to keep/order for row
modalities. If \code{NULL}, all observed levels are kept.}

\item{include_total}{Logical; include \code{Total} group if available.}

\item{drop_na}{Logical; if \code{TRUE}, remove rows with NA in row/group variable
before each cross-tabulation.}

\item{weights}{Optional weights. Either \code{NULL}, a numeric vector of length
\code{nrow(data)}, or a single column name in \code{data}.}

\item{rescale}{Logical; passed to \code{spicy::cross_tab()} to rescale weights.}

\item{correct}{Logical; passed to \code{spicy::cross_tab()} (Yates correction in
2x2 chi-squared contexts).}

\item{simulate_p}{Logical; passed to \code{spicy::cross_tab()}.}

\item{simulate_B}{Integer; Monte Carlo replicates when \code{simulate_p = TRUE}.}

\item{percent_digits}{Number of digits for percentages in report outputs.}

\item{p_digits}{Number of digits for p-values (except \verb{< .001}).}

\item{v_digits}{Number of digits for Cramer's V.}

\item{decimal_mark}{Decimal separator (\code{"."} or \code{","}).}

\item{output}{Output format: \code{"wide"}, \code{"long"}, \code{"tinytable"},
\code{"flextable"}, \code{"excel"}, \code{"clipboard"}, \code{"word"}.}

\item{style}{\code{"raw"} for machine-friendly outputs, \code{"report"} for formatted
outputs, \code{"auto"} to select by output type.}

\item{indent_text}{Prefix used for modality labels in report table building.}

\item{indent_text_excel_clipboard}{Stronger indentation used in Excel and
clipboard exports.}

\item{add_multilevel_header}{Logical; merge top headers in Excel export.}

\item{blank_na_wide}{Logical; replace NA by empty strings in wide raw output.}

\item{excel_path}{Path for \code{output = "excel"}.}

\item{excel_sheet}{Sheet name for Excel export.}

\item{clipboard_delim}{Delimiter for clipboard text export.}

\item{word_path}{Path for \code{output = "word"} or optional save path when
\code{output = "flextable"}.}
}
\value{
Depends on \code{output} and \code{style}:
\itemize{
\item \code{"long"} + \code{"raw"}: long numeric data frame.
\item \code{"wide"} + \code{"raw"}: wide numeric data frame.
\item \code{"long"} + \code{"report"}: long formatted character data frame.
\item \code{"wide"} + \code{"report"}: wide formatted character data frame.
\item \code{"tinytable"}: a \code{tinytable} object.
\item \code{"flextable"}: a \code{flextable} object.
\item \code{"excel"} / \code{"clipboard"} / \code{"word"}: invisibly returns written object/path.
}
}
\description{
\code{table_apa()} builds a publication-ready table by crossing one grouping
variable (\code{group_var}) with one or many row variables (\code{row_vars}), using
\code{spicy::cross_tab()} internally.
}
\details{
It supports raw data outputs (\code{wide}, \code{long}) and report-oriented outputs
(\code{tinytable}, \code{flextable}, \code{excel}, \code{clipboard}, \code{word}) with multi-level
headers, p-values, and Cramer's V.

Optional output engines require suggested packages:
\itemize{
\item \code{tinytable} for \code{output = "tinytable"}
\item \code{flextable} + \code{officer} for \code{output = "flextable"}/\code{"word"}
\item \code{openxlsx} for \code{output = "excel"}
\item \code{clipr} for \code{output = "clipboard"}
}
}
\examples{
# Build a minimal reproducible dataset
d_ex <- transform(
  mtcars,
  hes = factor(gear, labels = c("BFH", "HEdS-Geneve", "HESAV")),
  emploi_sf = ifelse(vs == 1, "Oui", "Non"),
  role_prof_recherche = ifelse(am == 1, "Oui", "Non"),
  w = mpg
)

# Raw long output (machine-friendly)
table_apa(
  data = d_ex,
  row_vars = c("emploi_sf", "role_prof_recherche"),
  group_var = "hes",
  labels = c("Emploi SF", "Role recherche"),
  output = "long",
  style = "raw"
)

# Raw wide output
table_apa(
  data = d_ex,
  row_vars = c("emploi_sf", "role_prof_recherche"),
  group_var = "hes",
  labels = c("Emploi SF", "Role recherche"),
  output = "wide",
  style = "raw"
)

# Weighted example
table_apa(
  data = d_ex,
  row_vars = c("emploi_sf", "role_prof_recherche"),
  group_var = "hes",
  labels = c("Emploi SF", "Role recherche"),
  weights = "w",
  rescale = TRUE,
  simulate_p = FALSE,
  output = "long",
  style = "raw"
)

\donttest{
# Optional output: tinytable
if (requireNamespace("tinytable", quietly = TRUE)) {
  table_apa(
    data = d_ex,
    row_vars = c("emploi_sf", "role_prof_recherche"),
    group_var = "hes",
    labels = c("Emploi SF", "Role recherche"),
    output = "tinytable"
  )
}

# Optional output: Excel
if (requireNamespace("openxlsx", quietly = TRUE)) {
  table_apa(
    data = d_ex,
    row_vars = c("emploi_sf", "role_prof_recherche"),
    group_var = "hes",
    labels = c("Emploi SF", "Role recherche"),
    output = "excel",
    excel_path = tempfile(fileext = ".xlsx")
  )
}
}
}
