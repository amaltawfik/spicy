% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assoc.R
\name{uncertainty_coef}
\alias{uncertainty_coef}
\title{Uncertainty Coefficient}
\usage{
uncertainty_coef(
  x,
  direction = c("symmetric", "row", "column"),
  detail = FALSE,
  conf_level = 0.95,
  digits = 3L
)
}
\arguments{
\item{x}{A contingency table (of class \code{table}).}

\item{direction}{Direction of prediction:
\code{"symmetric"} (default), \code{"row"} (column predicts row),
or \code{"column"} (row predicts column).}

\item{detail}{Logical. If \code{FALSE} (default), return the estimate
as a numeric scalar. If \code{TRUE}, return a named numeric vector
including confidence interval and p-value.}

\item{conf_level}{A number between 0 and 1 giving the confidence
level (default \code{0.95}). Only used when \code{detail = TRUE}. Set
to \code{NULL} to omit the confidence interval.}

\item{digits}{Number of decimal places used when printing the
result (default \code{3}). Only affects the \code{detail = TRUE} output.}
}
\value{
Same structure as \code{\link[=cramer_v]{cramer_v()}}: a scalar when
\code{detail = FALSE}, a named vector when \code{detail = TRUE}.
The p-value tests H0: U = 0 (Wald z-test).
}
\description{
\code{uncertainty_coef()} computes the Uncertainty Coefficient
(Theil's U) for a two-way contingency table, based on
information entropy.
}
\details{
The uncertainty coefficient measures association using Shannon
entropy. Let \eqn{H_X} and \eqn{H_Y} be the marginal entropies
of the \strong{row} and \strong{column} variables respectively, and
\eqn{H_{XY}} the joint entropy.
\itemize{
\item \code{direction = "row"} (column predicts row):
\eqn{U = (H_X + H_Y - H_{XY}) / H_X}.
\item \code{direction = "column"} (row predicts column):
\eqn{U = (H_X + H_Y - H_{XY}) / H_Y}.
\item \code{direction = "symmetric"}:
\eqn{U = 2 (H_X + H_Y - H_{XY}) / (H_X + H_Y)}.
}

The default \code{direction = "symmetric"} follows the SPSS and
DescTools convention: the symmetric coefficient is a standard,
well-defined variant with its own asymptotic standard error.
\code{\link[=somers_d]{somers_d()}} deliberately differs (its default is \code{"row"})
because its symmetric form is a derived quantity without an
analytic SE; see its documentation.

When the marginal entropy in the denominator is zero (the
predicted variable is constant, e.g. an unused factor level),
the coefficient is the undefined form \eqn{0/0}: the function
returns \code{NA} with a \code{spicy_undefined_stat} warning, like the
other measures in the family. For \code{direction = "symmetric"}
this happens only when both variables are constant; with a
single constant variable the symmetric coefficient is a
well-defined 0.

The entropy terms use the standard mathematical convention
\eqn{0 \log 0 = 0}, matching SPSS / PSPP \code{CROSSTABS} and the
definition in Cover & Thomas (2006). Note that
\code{DescTools::UncertCoef()} applies an additional Laplace
correction (replacing zero cells with \eqn{1/n^2}) before the
entropy computation, which produces slightly different point
estimates on tables with empty cells; that correction is
uncommon in the information-theory literature and is not used
here. The asymptotic standard errors follow the DescTools delta
method; see \code{\link[=cramer_v]{cramer_v()}} for full references.
}
\examples{
tab <- table(sochealth$smoking, sochealth$education)
uncertainty_coef(tab)
uncertainty_coef(tab, direction = "row", detail = TRUE)

}
\seealso{
\code{\link[=lambda_gk]{lambda_gk()}}, \code{\link[=goodman_kruskal_tau]{goodman_kruskal_tau()}}, \code{\link[=assoc_measures]{assoc_measures()}}

Other association measures:
\code{\link[=assoc_measures]{assoc_measures()}},
\code{\link[=contingency_coef]{contingency_coef()}},
\code{\link[=cramer_v]{cramer_v()}},
\code{\link[=gamma_gk]{gamma_gk()}},
\code{\link[=goodman_kruskal_tau]{goodman_kruskal_tau()}},
\code{\link[=kendall_tau_b]{kendall_tau_b()}},
\code{\link[=kendall_tau_c]{kendall_tau_c()}},
\code{\link[=lambda_gk]{lambda_gk()}},
\code{\link[=phi]{phi()}},
\code{\link[=somers_d]{somers_d()}},
\code{\link[=yule_q]{yule_q()}}
}
\concept{association measures}
