% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_regression_families.R
\name{table_regression_survival}
\alias{table_regression_survival}
\title{Survival models in table_regression()}
\description{
Reporting behaviour for \code{survival::coxph()} / \code{survreg()}, \code{rms::cph()},
and \code{flexsurv::flexsurvreg()} fits.
}
\section{Semantics}{

Cox models report hazard ratios under \code{exponentiate = TRUE} (the
canonical report; the footer carries the event count and concordance).
\code{survreg} accelerated-failure-time fits exponentiate to time ratios for
log-scale distributions; identity-scale distributions (gaussian,
logistic, t) are left untouched. AME is refused for Cox fits (no single
marginal-probability effect exists on the hazard scale; an
RMST-difference estimand is on the roadmap).
}

\section{Robust variance and nested comparison}{

\code{coxph}: \verb{CR*} via the Lin-Wei (1989) grouped-dfbeta sandwich, the
field-standard Cox robust SE, with subject-level cluster length checks
under censoring. \code{rms::cph} routes through \code{rms::robcov} (requires
\verb{x = TRUE, y = TRUE}). \code{nested = TRUE} compares nested Cox models with
the likelihood-ratio test (matching \code{anova.coxph}); R-squared-family
change statistics are not defined and stay empty.
}

\seealso{
\code{\link[=table_regression]{table_regression()}}, \link{table_regression_models}.
}
