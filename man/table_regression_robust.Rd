% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_regression_families.R
\name{table_regression_robust}
\alias{table_regression_robust}
\title{Robust, IV, quantile and panel models in table_regression()}
\description{
Reporting behaviour for \code{estimatr::lm_robust()} / \code{iv_robust()},
\code{AER::ivreg()} / \code{tobit()}, \code{quantreg::rq()}, and \code{fixest::feols()} /
\code{feglm()} / \code{fepois()} / \code{fenegbin()} fits.
}
\section{Semantics}{

\itemize{
\item \code{estimatr} fits carry their own robust SEs (including
per-coefficient CR2 Satterthwaite df); spicy reports them as fitted
and never overwrites them.
\item \code{quantreg::rq} honours the \verb{se =} method used at summary time;
rank-inversion CIs (\code{se = "rank"}) are carried into the CI columns.
\item \code{fixest} fits report their fixed-effects structure in the
footer; \code{feglm} / \code{fepois} / \code{fenegbin} exponentiate to OR / IRR.
}
}

\seealso{
\code{\link[=table_regression]{table_regression()}}, \link{table_regression_models}.
}
