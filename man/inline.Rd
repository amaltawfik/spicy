% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inline.R
\name{inline}
\alias{inline}
\title{Cite a table cell in inline text}
\usage{
inline(x, variable, level = NULL, column = NULL, model = NULL)
}
\arguments{
\item{x}{A table returned by \code{\link[=table_regression]{table_regression()}},
\code{\link[=table_categorical]{table_categorical()}}, \code{\link[=table_continuous]{table_continuous()}},
\code{\link[=table_continuous_lm]{table_continuous_lm()}}, \code{\link[=table_outcome]{table_outcome()}},
\code{\link[=table_categorical_svy]{table_categorical_svy()}} or \code{\link[=table_continuous_svy]{table_continuous_svy()}} (default
output) -- every family \code{\link[=as_structured]{as_structured()}} accepts.}

\item{variable}{The source variable, unquoted or as a string; or a
fit-statistic token (\code{"n"}, \code{"r2"}, ...).}

\item{level}{For a factor variable, the level, as a string.
\code{"(Missing)"} addresses the missing-value category by role. On
\code{\link[=table_continuous_lm]{table_continuous_lm()}}, whose groups are columns rather than
rows, it names the group.}

\item{column}{A column token, or a \code{{token}} pattern. \code{NULL} (the
default) returns the estimate-like column of the row when it is
unambiguous: the family's primary estimate. That is the
coefficient for \code{\link[=table_regression]{table_regression()}} -- always token \code{"b"}: an
exponentiated table changes its header to OR, IRR or HR, never
its token -- the contrast (\code{"delta"}) or, across a numeric \code{by},
the slope (\code{"b"}) for \code{\link[=table_continuous_lm]{table_continuous_lm()}}, the mean (\code{"m"})
or, on a median-only table, the median (\code{"med"}, \code{"med_iqr"}) for
\code{\link[=table_continuous]{table_continuous()}}, and the count (\code{"n"}) for
\code{\link[=table_categorical]{table_categorical()}}. A row carrying none of them refuses and
lists its tokens.}

\item{model}{In a multi-model table, the model: its label (as
displayed in the column spanners) or its position.}
}
\value{
A character scalar.
}
\description{
Returns one cell of a spicy table as a character scalar, formatted
exactly as the table displays it -- same decimals, same \emph{p} style,
same interval punctuation, same journal style. Designed for inline
R chunks in Quarto / R Markdown:

\if{html}{\out{<div class="sourceCode">}}\preformatted{Smokers had higher odds (`r inline(tbl, smoking, "Yes", "or")`).
}\if{html}{\out{</div>}}
}
\section{Addressing}{
The row is found by \strong{identity}, not by display text: \code{variable}
names the source column (\code{.variable} in the typed body), \code{level}
the level (\code{.level}). Custom \code{labels}, a \code{style}, or a translated
display never change the call. As a convenience, a \code{variable}
that matches no source column is looked up among the displayed
labels before erroring. The missing-value category is addressed by
\code{level = "(Missing)"} whatever its displayed (possibly deduplicated)
label, through its row role. Fit statistics are addressed by their
token as \code{variable} (\code{inline(tbl, "n")}, \code{inline(tbl, "r2")}).

A statistic that belongs to a whole variable rather than to one of
its levels sits on the variable's own row: the \emph{p} of a
\code{table_categorical()} block, its association measure, its SMD. Leave
\code{level} out to cite it (\code{inline(tbl, smoking, column = "p")}).

\code{\link[=table_continuous_lm]{table_continuous_lm()}} lays its groups out sideways -- one row per
outcome, the \code{by} levels as columns (\code{M (Female)}, \code{M (Male)}) --
so there \code{level} names the group whose column you want:
\code{inline(tbl, bmi, "Female", "emmean")}. The columns that belong to
no group (the contrast, its interval, \code{p}, \code{n}) are cited without
one, as before.

The column is a \strong{token} of the typed contract (\code{"b"}, \code{"se"},
\code{"p"}, \code{"ci"}, \code{"or"}, \code{"ame"}, \code{"n"}, \code{"pct"}, \code{"m"}, ... -- see
\code{as_structured()}'s \code{col_meta}), never a display header. \code{"ci"}
composes the interval with the style's brackets and separator, and
so does every other interval token the table carries (\code{"med_ci"},
\code{"ame_ci"}, \code{"assoc_ci"}): each addresses its own bounds. In
a multi-model table, \code{model} selects the model by its spanner
label or position; in a \code{by} table, the spanners are the groups,
so \code{model} selects the group the same way.
}

\section{Patterns}{
A \code{column} containing \verb{\{} is a pattern: each \code{{token}} is replaced
by the corresponding cell, so one call quotes a full sentence
fragment:

\if{html}{\out{<div class="sourceCode">}}\preformatted{inline(tbl, smoking, "Yes", "\{or\} (\{ci_label\} \{ci\}; p = \{p\})")
}\if{html}{\out{</div>}}

\code{{ci_label}} inserts the interval label of the interval the pattern
cites (\verb{95\% CI}, or \verb{Med 95\% CI} in a pattern quoting \code{{med_ci}}) --
the first one when it cites several, the table's first when it cites
none. Note that \code{{p}} carries the floor operator when the table does
(\verb{<.001}), so write \verb{p \{p\}} rather than \code{p = {p}} in patterns that
may hit the floor.
}

\section{Errors}{
Every misaddressing is a classed error that lists the available
choices: unknown variables list the variables, missing levels list
the levels, unknown tokens list the table's tokens, ambiguous
models list the spanner labels. A cell the table itself displays
as undefined (an aliased coefficient's en-dash) refuses with the
reason rather than pasting a dash into a sentence.
}

\examples{
fit <- lm(wellbeing_score ~ age + sex, data = sochealth)
tbl <- table_regression(fit)
inline(tbl, age, column = "b")
inline(tbl, sex, "Male", "{b} ({ci_label} {ci}; p {p})")
inline(tbl, "n")
}
\seealso{
\code{\link[=as_structured]{as_structured()}} for the typed contract behind the
addressing.
}
