% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_categorical_svy.R
\name{table_categorical_svy}
\alias{table_categorical_svy}
\title{Categorical summary table from a survey design}
\usage{
table_categorical_svy(
  design,
  select = tidyselect::everything(),
  by = NULL,
  labels = NULL,
  levels_keep = NULL,
  include_total = TRUE,
  drop_na = FALSE,
  proportion_ci = FALSE,
  ci_method = c("logit", "likelihood", "asin", "beta", "mean", "xlogit", "wilson"),
  ci_level = 0.95,
  chisq_statistic = c("F", "Chisq", "Wald", "adjWald", "saddlepoint"),
  deff = FALSE,
  df = NULL,
  p_value = NULL,
  percent_digits = 1,
  p_digits = 3,
  decimal_mark = ".",
  align = c("decimal", "center", "right"),
  output = c("default", "data.frame", "long", "tinytable", "gt", "flextable", "excel",
    "clipboard", "word"),
  indent_text = "  ",
  indent_text_excel_clipboard = strrep(" ", 6),
  excel_path = NULL,
  excel_sheet = NULL,
  clipboard_delim = "\\t",
  word_path = NULL,
  user_na = TRUE,
  style = NULL
)
}
\arguments{
\item{design}{A survey design: \code{survey::svydesign()} or
\code{survey::as.svrepdesign()}.}

\item{select}{Columns to tabulate, as a tidyselect expression on the
design's variables.}

\item{by}{A single grouping column: one column block per level.}

\item{labels}{Named character vector of display labels.}

\item{levels_keep}{Levels to keep, as a character vector (all
variables) or a named list (per variable).}

\item{include_total}{Add a \code{Total} column block with the whole
design's percentages (default \code{TRUE}, only with \code{by}).}

\item{drop_na}{Drop missing values (default \code{FALSE}: they show as a
\code{(Missing)} level). Shown or dropped, they never enter the test:
the p-value is computed on the complete cases either way, which is
the convention \code{\link[=table_categorical]{table_categorical()}} applies.}

\item{proportion_ci}{Add the confidence interval of each percentage.}

\item{ci_method}{Interval method passed to \code{survey::svyciprop()}:
\code{"logit"} (default), \code{"likelihood"}, \code{"asin"}, \code{"beta"},
\code{"mean"}, \code{"xlogit"} or \code{"wilson"}.}

\item{ci_level}{Coverage of the interval.}

\item{chisq_statistic}{Statistic for \code{survey::svychisq()}: \code{"F"}
(default), \code{"Chisq"}, \code{"Wald"}, \code{"adjWald"} or \code{"saddlepoint"}.}

\item{deff}{Show the design effect of each percentage: \code{FALSE}
(default), \code{TRUE} or \code{"replace"}.}

\item{df}{Degrees of freedom for the intervals. \code{NULL} (default)
uses \code{survey::degf()} on each domain. It does not reach the test:
\code{survey::svychisq()} has no \code{df} argument, so the Rao-Scott
reference distribution keeps the design's own degrees of freedom
and the note says so.}

\item{p_value}{Show the p-value column (defaults to \code{TRUE} with
\code{by}).}

\item{percent_digits, p_digits, decimal_mark}{Number formatting.}

\item{align}{Numeric-cell alignment: \code{"decimal"}, \code{"center"} or
\code{"right"}.}

\item{output}{One of \code{"default"}, \code{"data.frame"}, \code{"long"}, or a
rendering engine: \code{"tinytable"}, \code{"gt"}, \code{"flextable"},
\code{"excel"}, \code{"clipboard"}, \code{"word"}.}

\item{indent_text, indent_text_excel_clipboard}{Level-row
indentation, for the console and for the plain-text engines.}

\item{excel_path, excel_sheet, clipboard_delim, word_path}{Output
destinations, as in \code{\link[=table_categorical]{table_categorical()}}.}

\item{user_na}{Honour declared missing values (see \code{?freq}).}

\item{style}{A journal style; see \code{\link[=spicy_style]{spicy_style()}}.}
}
\value{
A \code{spicy_categorical_svy_table}: the wide compute frame,
with the display frame and the typed view attached.
\code{output = "data.frame"} / \code{"long"} returns the compute frame
unclassed.
}
\description{
The design twin of \code{\link[=table_categorical]{table_categorical()}}: counts and estimated
percentages of categorical variables computed from a
\code{survey::svydesign()} or \code{survey::as.svrepdesign()} object instead
of a data frame.

Every statistic is survey's. \code{survey::svymean()} estimates the
percentages and their design effects, \code{survey::svyciprop()} their
confidence intervals, and \code{survey::svychisq()} tests the
association, Rao-Scott corrected and referred to the design degrees
of freedom.
}
\section{What the columns are}{
\code{n} is the \emph{observed} count -- the number of rows in the sample, not
an estimated population size. \verb{\%} is the estimated percentage
\emph{within} its column: without \code{by} it is the distribution of the
variable in the population, with \code{by} the distribution inside that
domain. The table note gives the sample size and the estimated
population together, because neither alone tells the reader what
they are looking at.

\code{proportion_ci = TRUE} adds the interval of each percentage.
\code{ci_method} chooses among the seven \code{survey::svyciprop()} offers;
the default \code{"logit"} is bounded inside 0 to 100, which the Wald
interval (\code{"mean"}) is not. The percentage itself always comes from
\code{survey::svymean()}, so it does not move when \code{ci_method} does.
}

\section{The test}{
\code{svychisq()} with \code{chisq_statistic = "F"} (the default) is the
Pearson chi-square with the Rao-Scott second-order correction,
referred to F(ndf, \code{survey::degf(design)}). It is survey's own
default and the one Stata's \code{svy: tabulate} reports.

It runs on the \emph{complete cases} of the two variables, and on their
observed levels: a \code{(Missing)} row and a declared-but-unobserved
level are descriptive, and neither belongs to the null hypothesis.
The p-value is therefore the same whether \code{drop_na} shows those rows
or removes them, and the intervals beside it describe the same
domain -- the two families test the same table.

\code{"Chisq"} shows the p-value only: survey adjusts the statistic in
the \code{"F"} branch and only the p-value in the \code{"Chisq"} one, so the
statistic there is not the one the p-value came from. \code{"Wald"},
\code{"adjWald"} and \code{"saddlepoint"} are available; \code{"lincom"} and
\code{"wls-score"} are refused, the first because its integration is
documented as failing in the far tail (\code{?pchisqsum}), the second
because it has no reporting convention here.
}

\section{What is absent, and why}{
\code{weights} and \code{rescale} (the weighting \emph{is} the design). \code{correct}
(Yates), \code{simulate_p} and \code{simulate_B}, which have no meaning once
the reference distribution is Rao-Scott's. And the association
measures: Cramer's V, phi, tau-b/c, gamma, Somers' D and lambda
have no established design-based variance, and the intervals
\code{\link[=table_categorical]{table_categorical()}} gives them assume simple random sampling. The
design-based measure of association here is the Rao-Scott test in
the \code{p} column; for an effect size, model it with
\code{table_regression(survey::svyglm(...))}.
}

\examples{
\dontshow{if (requireNamespace("survey", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(api, package = "survey")
dclus1 <- survey::svydesign(
  id = ~dnum, weights = ~pw, data = apiclus1, fpc = ~fpc
)
table_categorical_svy(dclus1, select = c(stype, awards))
table_categorical_svy(dclus1, select = stype, by = sch.wide)
table_categorical_svy(
  dclus1,
  select = stype,
  proportion_ci = TRUE,
  deff = TRUE
)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=table_categorical]{table_categorical()}} for the data-frame sibling,
\code{\link[=table_continuous_svy]{table_continuous_svy()}} for continuous variables.
}
