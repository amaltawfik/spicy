% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assoc.R
\name{assoc_measures}
\alias{assoc_measures}
\title{Association measures summary table}
\usage{
assoc_measures(
  x,
  type = c("all", "nominal", "ordinal"),
  conf_level = 0.95,
  digits = 3L
)
}
\arguments{
\item{x}{A contingency table (of class \code{table}).}

\item{type}{Which family of measures to compute:
\code{"all"} (default), \code{"nominal"}, or \code{"ordinal"}.}

\item{conf_level}{A single number strictly between 0 and 1 giving
the confidence level (default \code{0.95}). Set to \code{NULL} to omit
the confidence interval. Any other value -- including
percentages such as \code{95} -- raises a classed error
(\code{spicy_invalid_input}).}

\item{digits}{Number of decimal places used when printing the
result (default \code{3}).}
}
\value{
A data frame with columns \code{measure}, \code{estimate}, \code{se},
\code{ci_lower}, \code{ci_upper}, and \code{p_value}. The \code{p_value} comes
from two test families:
\itemize{
\item \strong{Pearson chi-squared test of independence} for
Cramer's V, Phi, and the Contingency Coefficient (the
three chi-squared-derived nominal measures). All three
carry the same chi-squared \emph{p}-value on a given table.
\item \strong{Wald z-test of H0: measure = 0} for every other
measure: Yule's Q, Lambda, Goodman-Kruskal's Tau, the
Uncertainty Coefficient, and all ordinal measures
(Gamma, Tau-b, Tau-c, Somers' D).
}
Direction-dependent measures (\code{lambda_gk()},
\code{goodman_kruskal_tau()}, \code{uncertainty_coef()}, \code{somers_d()})
contribute one row per direction (\code{symmetric} / \code{R|C} / \code{C|R}
where applicable), so the output has more rows than the
number of helper functions.
}
\description{
\code{assoc_measures()} computes a range of association measures for a
two-way contingency table and returns them in a tidy data frame.
}
\details{
\code{type = "all"} (the default) returns all nominal and ordinal
measures. Use \code{type = "nominal"} or \code{type = "ordinal"} to
restrict the output to a single family.

The nominal family includes \code{\link[=cramer_v]{cramer_v()}}, \code{\link[=contingency_coef]{contingency_coef()}},
\code{\link[=lambda_gk]{lambda_gk()}}, \code{\link[=goodman_kruskal_tau]{goodman_kruskal_tau()}}, \code{\link[=uncertainty_coef]{uncertainty_coef()}},
and (for 2x2 tables) \code{\link[=phi]{phi()}} and \code{\link[=yule_q]{yule_q()}}.

The ordinal family includes \code{\link[=gamma_gk]{gamma_gk()}}, \code{\link[=kendall_tau_b]{kendall_tau_b()}},
\code{\link[=kendall_tau_c]{kendall_tau_c()}}, and \code{\link[=somers_d]{somers_d()}}.

Measures that are undefined on the given table appear as \code{NA}
rows (printed as \verb{--}). The classed warnings the individual
functions raise (e.g. \code{spicy_undefined_stat}) are re-emitted
once per distinct message after the table is assembled, so
condition handlers and \code{suppressWarnings()} behave as they do
for the individual functions.

Standard error formulas follow the DescTools implementations
(Signorell et al., 2024), except for Kendall's Tau-b, whose
ASE follows Brown and Benedetti (1977) as printed by SPSS /
PSPP \code{CROSSTABS}; see \code{\link[=kendall_tau_b]{kendall_tau_b()}}.
}
\examples{
tab <- table(sochealth$smoking, sochealth$education)
assoc_measures(tab)
assoc_measures(tab, type = "nominal")
assoc_measures(tab, type = "ordinal")

}
\references{
Agresti, A. (2002). \emph{Categorical Data Analysis} (2nd ed.). Wiley.

Brown, M. B., & Benedetti, J. K. (1977). Sampling behavior of
tests for correlation in two-way contingency tables. \emph{Journal of
the American Statistical Association}, 72(358), 309-315.

Liebetrau, A. M. (1983). \emph{Measures of Association}. Sage.

Signorell, A. et al. (2024). \emph{DescTools: Tools for Descriptive
Statistics}. R package.
}
\seealso{
\code{\link[=cramer_v]{cramer_v()}}, \code{\link[=gamma_gk]{gamma_gk()}}, \code{\link[=kendall_tau_b]{kendall_tau_b()}}

Other association measures:
\code{\link[=contingency_coef]{contingency_coef()}},
\code{\link[=cramer_v]{cramer_v()}},
\code{\link[=gamma_gk]{gamma_gk()}},
\code{\link[=goodman_kruskal_tau]{goodman_kruskal_tau()}},
\code{\link[=kendall_tau_b]{kendall_tau_b()}},
\code{\link[=kendall_tau_c]{kendall_tau_c()}},
\code{\link[=lambda_gk]{lambda_gk()}},
\code{\link[=phi]{phi()}},
\code{\link[=somers_d]{somers_d()}},
\code{\link[=uncertainty_coef]{uncertainty_coef()}},
\code{\link[=yule_q]{yule_q()}}
}
\concept{association measures}
