% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_regression.R
\name{table_regression}
\alias{table_regression}
\title{Regression coefficient summary table}
\usage{
table_regression(
  models,
  vcov = "classical",
  cluster = NULL,
  ci_level = 0.95,
  ci_method = c("wald", "profile"),
  boot_n = 1000L,
  standardized = c("none", "refit", "posthoc", "basic", "smart", "pseudo"),
  exponentiate = FALSE,
  p_adjust = "none",
  show_columns = NULL,
  keep = NULL,
  drop = NULL,
  show_intercept = TRUE,
  intercept_position = c("first", "last"),
  factor_layout = c("grouped", "flat"),
  reference_style = c("row", "annotation", "footer", "none"),
  reference_label = "(ref.)",
  show_fit_stats = NULL,
  fit_stats_layout = c("first_col", "merged"),
  model_labels = NULL,
  outcome_labels = NULL,
  stars = FALSE,
  nested = FALSE,
  digits = 2L,
  p_digits = 3L,
  effect_size_digits = 2L,
  fit_digits = 2L,
  ic_digits = 1L,
  decimal_mark = ".",
  align = c("decimal", "center", "right", "auto"),
  padding = 0L,
  labels = NULL,
  title = NULL,
  note = NULL,
  output = c("default", "data.frame", "long", "gt", "flextable", "tinytable", "excel",
    "clipboard", "word"),
  excel_path = NULL,
  excel_sheet = "Regression",
  clipboard_delim = "\\t",
  word_path = NULL
)
}
\arguments{
\item{models}{An \code{lm} or \code{glm} fitted model, or a list of such
fits (named or unnamed; \code{lm} and \code{glm} may be mixed). Single
fits are auto-promoted to a 1-element list. \code{merMod} and
other classes raise \code{spicy_unsupported}. Raw data + formula
is not accepted -- fit-only API.}

\item{vcov}{Variance-covariance estimator: \code{"classical"},
\code{"HC0"}-\code{"HC5"}, \code{"CR0"}-\code{"CR3"}, \code{"bootstrap"}, or
\code{"jackknife"}. A scalar is recycled to all models; a list
(one string per model) allows mixed estimators. Default
\code{"classical"}. See \emph{Inference and standard errors}.}

\item{cluster}{Cluster identifier for cluster-robust variance
(used when \code{vcov} is \code{"CR0"}-\code{"CR3"} or a cluster-bootstrap /
cluster-jackknife). Three accepted forms (see \emph{How to specify
\code{cluster}} in the details):
\itemize{
\item Formula: \code{~region}, \code{~region:year} (recommended).
\item String column name: \code{"region"}.
\item Atomic vector of length \code{nobs(fit)}: \code{df$region},
\code{interaction(df$region, df$year)}, ... (for keys derived
on the fly).
}
For multi-model use, pass a list of one form per model
(mix-and-match allowed). Bare unquoted names
(\code{cluster = region}) are NOT accepted -- use \code{~region} or
\code{"region"}. Default \code{NULL} (no clustering).}

\item{ci_level}{Confidence level for all reported CIs (B, \eqn{\beta}{beta},
AME, partial effect sizes). Default \code{0.95}.}

\item{ci_method}{CI construction. \code{"wald"} (default) uses
\verb{estimate +/- z x SE} (\verb{t x SE} for \code{lm}). \code{"profile"}
(\emph{glm only}) uses the profile-likelihood CI from
\code{\link[MASS:confint.glm]{MASS::confint.glm()}} -- asymmetric, exact for
likelihood-based inference (Venables & Ripley \emph{MASS} Section 7.2).
Only the CI bounds change; estimate, SE, statistic and
p-value remain Wald. \code{"profile"} with \code{lm} raises
\code{spicy_invalid_input}.}

\item{boot_n}{Number of bootstrap replicates when
\code{vcov = "bootstrap"}. Single positive integer. Default
\code{1000L}.}

\item{standardized}{Standardisation method for the \code{"beta"}
column. One of \code{"none"} (default), \code{"refit"}, \code{"posthoc"},
\code{"basic"}, \code{"smart"}, \code{"pseudo"}. \code{"pseudo"} is \emph{glm only}
(Menard 2011 fully-standardised); using it with \code{lm()} raises
\code{spicy_invalid_input}. See the \emph{Standardised coefficients}
section.}

\item{exponentiate}{Logical. When \code{TRUE} and the model is a
\code{glm} with a non-identity link, \code{B}, the CI bounds, and the
SE are transformed via \code{exp()} (delta method:
\verb{SE_OR = OR x SE_log-odds}). The column header is rebranded
per family / link: \code{OR} (binomial logit), \code{IRR} (poisson
log), \code{HR} (binomial cloglog), \code{RR} (binomial log), \code{MR}
(Gamma log), else \code{exp(B)}. The statistic and p-value stay
on the link scale (invariant under monotone transformation).
Default \code{FALSE}. No effect on \code{lm} or identity-link glm;
emits a \code{spicy_ignored_arg} warning in those cases.}

\item{p_adjust}{Multiple-comparison adjustment method applied
to the family of estimated coefficient p-values within each
model (intercept and reference rows excluded). One of
\code{"none"} (default), \code{"holm"}, \code{"hochberg"}, \code{"hommel"},
\code{"bonferroni"}, \code{"BH"} / \code{"fdr"}, or \code{"BY"}. Delegated to
\code{\link[stats:p.adjust]{stats::p.adjust()}}; applied per-model and per \code{estimate_type}
(B and AME p-values are adjusted independently within their
own families). Active adjustments are documented in the
footer (method + family size). See the \emph{Multiple-comparison
adjustment} section for when this is and is not appropriate.}

\item{show_columns}{Character vector of tokens selecting the
per-coefficient columns and their display order. Accepts
\strong{atomic tokens} (\code{"b"}, \code{"se"}, \code{"ci"}, \code{"t"}, \code{"p"},
\code{"beta"}, \code{"ame"}, \code{"ame_se"}, \code{"ame_ci"}, \code{"ame_p"},
\code{"partial_f2"} + \code{"partial_f2_ci"}, \code{"partial_eta2"} +
\code{"partial_eta2_ci"}, \code{"partial_omega2"} +
\code{"partial_omega2_ci"}, \code{"partial_chi2"}) and \strong{group tokens}
(\code{"all_b"}, \code{"all_b_compact"}, \code{"all_b_full"}, \code{"all_beta"},
\code{"all_ame"}, \code{"all_ame_compact"}, \code{"all_f2"}, \code{"all_eta2"},
\code{"all_omega2"}). See \emph{Vocabulary tokens} in the details for
the full enumeration. Default \code{NULL} selects a context-aware
layout: \code{"all_b"} (single model) or \code{"all_b_compact"} (multi-
model). The \code{"p"} token is always the B / beta p-value; for
the AME-specific p-value use \code{"ame_p"}.}

\item{keep}{Character vector of regexes. Only coefficient rows
whose term name (as in \code{\link[stats:coef]{stats::coef()}} -- e.g. \code{"wt"},
\code{"cyl6"}, \code{"factor(cyl)8"}) matches at least one pattern are
kept. Mutually exclusive with \code{drop}. Filtering is a display
choice; \code{p_adjust} runs against the full coefficient family
before filtering. Default \code{NULL} (no filter).}

\item{drop}{Character vector of regexes. Coefficient rows
matching any pattern are removed. Mutually exclusive with
\code{keep}. Default \code{NULL}.}

\item{show_intercept}{Whether to display the intercept row.
Default \code{TRUE} (APA convention). Hide via \code{FALSE}.}

\item{intercept_position}{Where to place the intercept when
shown. \code{"first"} (default, APA) or \code{"last"} (Stata-style,
intercept just above the fit-stats footer). Ignored when
\code{show_intercept = FALSE} (with \code{spicy_ignored_arg} warning).}

\item{factor_layout}{Layout of factor predictors. Applies to
\strong{any categorical predictor} -- \code{factor}, \code{ordered},
\code{character}, or \code{logical} (R coerces the latter two to
factors at fit time). Two options:
\itemize{
\item \code{"grouped"} (default): the variable name on its own
header row ending with \code{:} (e.g., \verb{education:}); each
level follows as an indented sub-row with the bare level
name. APA convention.
\item \code{"flat"}: each non-reference dummy is one row with
the \verb{<variable><level>} form (e.g., \code{educationUpper}); no
header, no indent. Econometrics convention.
}}

\item{reference_style}{Rendering of factor reference levels.
Four modes, distinguishing WHERE the reference information is
exposed (in a row, inline, in the footer, or nowhere):
\itemize{
\item \code{"row"} (default): explicit row \code{Female (ref.)} with
em-dashes in all stat columns (NEJM / BMJ clinical
convention). \code{reference_label} controls the suffix.
\item \code{"annotation"}: the row is dropped and the reference
is shown inline. Under \code{factor_layout = "grouped"} the
factor header reads \verb{education: [ref: Lower]}; under
\code{factor_layout = "flat"} the marker \verb{[vs Lower]} is
attached to the \strong{first non-reference dummy} of each
factor (subsequent dummies inherit the same reference).
\item \code{"footer"}: the row is dropped and a single line
\verb{Reference categories: education = Lower; sex = Female.}
is added to the footer note. SAS \verb{PROC LOGISTIC} / SPSS
"Categorical Variables Codings" convention. Best for
publication-grade dense multi-factor tables.
\item \code{"none"}: the row is dropped and no reference
information is displayed anywhere. The user is
responsible for stating the reference convention
elsewhere (article text, table caption). Under
\code{factor_layout = "flat"}, an informational message is
emitted to flag the silent omission.
}}

\item{reference_label}{Suffix shown after the reference level in
\code{reference_style = "row"} mode. Default \code{"(ref.)"}. Ignored
by the other three modes (which use structural English
wording -- "ref:", "vs", "Reference categories:").}

\item{show_fit_stats}{Character vector of tokens for the
model-level rows below the coefficients; row order follows
token order. \code{NULL} (default) resolves class-aware:
\itemize{
\item \code{lm}: \code{c("nobs", "r2", "adj_r2")}.
\item \code{glm}: \code{c("nobs", "pseudo_r2_mcfadden", "pseudo_r2_nagelkerke", "AIC")}.
\item mixed \code{lm} + \code{glm}: the union of the two (the
renderer em-dashes per cell the stat not defined for a
given model class).
}
Under \code{nested = TRUE} the default is extended with the
class-appropriate change-stat tokens (e.g. \code{"r2_change"},
\code{"f_change"} for \code{lm}). See \emph{Vocabulary tokens}
(\code{show_fit_stats} subsection) and \emph{Hierarchical (nested)
model comparison} in the details for the full vocabulary.}

\item{fit_stats_layout}{Layout of the fit-stat values (\code{n}, \code{R^2},
\code{AIC}, ...) within each model's column group. Two options:
\itemize{
\item \code{"first_col"} (default): the value is placed in the
FIRST numeric sub-column of each model (typically \code{B});
the model's remaining sub-columns (\code{SE}, \code{LL}, \code{UL}, \code{p},
...) are left empty for that row. APA convention (Table
7.13) and the layout used by \code{gtsummary}, \code{modelsummary},
\code{sjPlot::tab_model}, \code{jtools}, \code{parameters}.
\item \code{"merged"}: the model's numeric sub-columns are
merged into a single wide cell containing the fit-stat
value, centred under the model spanner. Stata \code{esttab} /
Econometrica / AER convention. Resolves the mixed-
precision look of \code{"first_col"} (an integer \code{n} row
sharing the B column with two-decimal coefficients).
}
Cell merging is supported by \code{excel}, \code{flextable}, and
\code{word} (via flextable). \code{gt}, \code{tinytable}, \code{clipboard}, and
\code{default} (console) always render in \code{"first_col"} mode
regardless of this setting:
\itemize{
\item \code{gt} lacks a native row-spanning cell-merge API
(\code{tab_spanner} covers columns, not row-cell ranges).
\item \code{tinytable}'s \code{style_tt(colspan = N)} emits HTML
\code{colspan} only on header rows, not on body cells.
\item \code{clipboard} ships TSV plaintext.
\item \code{default} ships fixed-width ASCII.
}
Decimal alignment of every numeric column is preserved in
both modes: the \code{B} column decimal-aligns its coefficient
values plus any fit-stat value(s) in \code{"first_col"} mode
(native primitives handle the mixed-precision case), and
trivially decimal-aligns in \code{"merged"} mode (the fit-stat
values move out of the B column into the merged cell).}

\item{model_labels}{Per-model labels used as the \strong{column-group
spanner} above each model's sub-columns (console + gt /
flextable / tinytable / Excel / Word renderers). \code{NULL}
(default) resolves automatically; see \emph{Multi-model semantics}
for the full rule. A character vector of length
\code{length(models)} overrides.}

\item{outcome_labels}{Optional \strong{Outcome body row} override.
\code{NULL} (default) hides the row entirely -- under the
multi-model spanner the DV is already visible above the data.
A character vector of length \code{length(models)} forces an
explicit Outcome row with those values (the spanner stays as
\code{"Model 1, ..."} unless \code{model_labels} is also supplied).
\code{FALSE} also suppresses the row.}

\item{stars}{Significance asterisks. \code{FALSE} (default, APA 7
Section 6.46) -- no stars. \code{TRUE} -- APA cutoffs
\code{c("*" = 0.05, "**" = 0.01, "***" = 0.001)}. A named numeric
vector specifies custom thresholds, e.g.
\code{c("+" = 0.10, "*" = 0.05, "**" = 0.01, "***" = 0.001)}.}

\item{nested}{Whether to inject pairwise change-statistic rows
for adjacent models (M2 vs M1, M3 vs M2, ...). \code{FALSE}
(default) -- pure side-by-side display. \code{TRUE} -- requires
identical \code{nobs} and identical response variable across all
models. See \emph{Hierarchical (nested) model comparison}.}

\item{digits}{Decimal places for general numeric tokens
(\code{b}, \code{beta}, \code{se}, \code{ci}, \code{t}, \code{f_change}, \code{lrt_change},
\code{deviance}, \code{deviance_change}, \code{ame}, \code{ame_se},
\code{weighted_nobs}). Default \code{2L}.}

\item{p_digits}{Decimal places for p-values (\code{p}, \code{ame_p},
\code{p_change}). APA-strict: leading zero stripped, \verb{<.001} (or
\verb{<.0001} etc. depending on \code{p_digits}) for small values.
Default \code{3L}.}

\item{effect_size_digits}{Decimals for per-coefficient effect
sizes (\code{partial_f2}, \code{partial_eta2}, \code{partial_omega2}).
Default \code{2L}.}

\item{fit_digits}{Decimals for variance-explained / model-level
effect-size fit stats (\code{r2}, \code{adj_r2}, \code{r2_change},
\code{adj_r2_change}, \code{omega2}, \code{f2}, \code{f2_change}, \code{sigma},
\code{rmse}). Default \code{2L}.}

\item{ic_digits}{Decimals for information criteria (\code{AIC},
\code{AICc}, \code{BIC}, and their \verb{_change} form). Default \code{1L}.}

\item{decimal_mark}{Decimal mark used in numeric display.
\code{"."} (default) or \code{","} (European convention). When
\code{","} is used, the CI bracket separator switches to \code{"; "}
automatically to avoid \code{"0,18 [0,07, 0,30]"} ambiguity.}

\item{align}{Numeric column alignment.
\code{"decimal"} (default) -- pre-pad cells so decimal marks line
up vertically (publication-style). For CI cells (\verb{[LL, UL]})
the left bracket, the LL decimal point, the comma separator,
the UL decimal point, and the right bracket are independently
aligned across rows.
\code{"center"}, \code{"right"}, or \code{"auto"} for legacy per-column
alignment.}

\item{padding}{Non-negative integer giving the extra characters
added to each data column's auto-computed width when the
default \code{print} method renders the table. Default \code{0L}
(compact -- fits more models in the same console / page
width). Use \code{2L} (Stata-like) or \code{4L} for a more spacious
look. Headers stay centered above the data region regardless
of padding.}

\item{labels}{Named character vector overriding per-coefficient
row labels. Names are coefficient term names (from
\code{\link[stats:terms]{stats::terms()}}); values are the displayed labels. E.g.
\code{c("age" = "Age (years)", "sexM" = "Male (vs Female)")}.
Default \code{NULL} (use raw term names).}

\item{title, note}{Override or suppress the auto-built caption /
methodological footer. Three modes per argument:
\itemize{
\item \code{NULL} (default): the package builds the standard caption
("Linear regression on \verb{<DV>}" / "Hierarchical linear
regression on \verb{<DV>}" / ...) and a methodological note
(VCV type, p-adjust method, reference categories, ...).
\item \code{FALSE}: the corresponding banner row is omitted from
every output engine. Use when the surrounding manuscript
provides its own caption / note.
\item character string (length 1): replaces the auto-built
text verbatim. The renderer applies no APA formatting on
top -- supply the exact string you want displayed (multi-
line notes accepted via embedded \code{"\\n"}).
}
Validation messages, the spanner row, and the in-body change-
stat rows are \emph{not} affected -- they belong to the table
structure, not to the banner.}

\item{output}{Output type. \code{"default"} (a printable
\code{spicy_regression_table}); \code{"data.frame"} / \code{"long"} (raw
data); \code{"gt"} / \code{"flextable"} / \code{"tinytable"} (rich-format
tables); \code{"excel"} (writes to \code{excel_path}); \code{"clipboard"}
(copies to system clipboard); \code{"word"} (writes flextable to
\code{word_path}).}

\item{excel_path}{File path for \code{output = "excel"}. Default
\code{NULL} (required when \code{output = "excel"}).}

\item{excel_sheet}{Sheet name when writing to Excel. Default
\code{"Regression"}.}

\item{clipboard_delim}{Field delimiter for
\code{output = "clipboard"}. Default \code{"\\t"} (tab-separated, pastes
cleanly into Excel / Google Sheets). The clipboard payload
mirrors the Excel layout (title row, spanner row, header,
body, footer note) but is plain text -- horizontal rules,
cell merging, decimal alignment, monospace font, and factor-
level indentation cannot be encoded in TSV and are therefore
absent from the paste. For a fully-formatted result, use
\code{output = "excel"} or \code{output = "word"} and open / paste from
the produced file.}

\item{word_path}{File path for \code{output = "word"}. Default
\code{NULL} (required when \code{output = "word"}).}
}
\value{
A \code{spicy_regression_table} object (a \code{data.frame}
subclass with classes \code{c("spicy_regression_table", "spicy_table", "data.frame")}) when \code{output = "default"}.
The result carries rendering attributes (\code{title}, \code{note},
\code{align}, \code{padding}) and provenance attributes (\code{outcome},
\code{model_ids}) consumed by the print method and the broom
methods. For other \code{output} values, returns the
format-specific object (\code{gt_tbl}, \code{flextable}, \code{tinytable},
\code{data.frame}, \code{tbl_df}, or \code{invisible(x)} for side-effect
outputs).
}
\description{
Publication-ready coefficient table from one or more fitted
\code{lm} / \code{glm} models. Supports standardised coefficients
(\eqn{\beta}{beta}), average marginal effects (AME), partial
effect sizes (\emph{\eqn{f^2}{f^2}} / \emph{\eqn{\eta^2}{eta^2}} /
\emph{\eqn{\omega^2}{omega^2}} for \code{lm}; partial
\emph{\eqn{\chi^2}{chi-squared}} for \code{glm}), pseudo-\eqn{R^2}{R^2}
(\code{glm}), and a full vocabulary of variance estimators
(classical / HC* / cluster-robust with Satterthwaite-corrected
df / bootstrap / jackknife). \code{glm} covers binomial / poisson /
Gamma / inverse.gaussian / quasi families with any link.
}
\section{Vocabulary tokens}{
Two vector arguments -- \code{show_columns} and \code{show_fit_stats} --
accept named tokens that select \strong{what} to display and in
\strong{what order}. All tokens are lowercase
(snake_case for compound tokens). Group tokens
(\code{"all_b"}, \code{"all_ame"}, ...) expand to a fixed vector of
atomic tokens; see \code{show_columns} below.
\subsection{\code{show_columns} -- per-coefficient columns}{

Each token = one displayed column.

\itemize{
\item Coefficient family: \code{"b"}, \code{"beta"} (standardised),
\code{"se"}, \code{"ci"}, \code{"t"}, \code{"p"}.
\item Marginal effects: \code{"ame"}, \code{"ame_se"}, \code{"ame_ci"},
\code{"ame_p"}. \code{"p"} always refers to the B-coefficient
p-value; for the AME-specific p-value use \code{"ame_p"}.
\item Partial effect sizes -- \code{lm} only: \code{"partial_f2"},
\code{"partial_eta2"}, \code{"partial_omega2"}, each with a paired
\verb{_ci} companion (\code{"partial_f2_ci"}, ...).
\item Partial effect size -- \code{glm} only: \code{"partial_chi2"}
(likelihood-ratio chi-square via \code{drop1(test = "LRT")};
SAS PROC LOGISTIC \code{TYPE3}; Long & Freese 2014 Section 3.5).
Rendered as \code{value (df)} to disambiguate factor terms
(k-1 df) from numeric terms (1 df).
}

\strong{Group tokens} (presets) expand to a fixed atomic vector
before validation:

\itemize{
\item \code{"all_b"}         -> \code{c("b", "se", "ci", "p")}
\item \code{"all_b_compact"} -> \code{c("b", "se", "p")}
\item \code{"all_b_full"}    -> \code{c("b", "se", "ci", "t", "p")}
\item \code{"all_beta"}      -> \code{c("b", "beta", "se", "ci", "p")}
\item \code{"all_ame"}       -> \code{c("ame", "ame_se", "ame_ci", "ame_p")}
\item \code{"all_ame_compact"} -> \code{c("ame", "ame_p")}
\item \code{"all_f2"} / \code{"all_eta2"} / \code{"all_omega2"} -> \verb{partial_*}
+ its \verb{_ci} companion.
}

Mix groups and atomic tokens:
\code{show_columns = c("all_b", "ame", "ame_p")}. Duplicates after
expansion are deduplicated; the order of tokens controls the
order of the displayed columns. If \code{standardized != "none"} and
\code{"beta"} is not already requested, it is auto-injected after
\code{"b"}. Asking for \code{"beta"} while \code{standardized = "none"} raises
\code{spicy_invalid_input}.

\strong{Default} (\code{show_columns = NULL}) is context-aware:
\code{"all_b"} for a single model (APA-7 Section 6.46 publication layout),
\code{"all_b_compact"} for two or more models (CI dropped to fit the
side-by-side layout; restore it explicitly when needed).
}

\subsection{\code{show_fit_stats} -- model-level rows below the coefficients}{

\itemize{
\item Counts: \code{"nobs"}, \code{"weighted_nobs"}.
\item Variance explained (\code{lm} only): \code{"r2"}, \code{"adj_r2"},
\code{"omega2"}.
\item Pseudo-\eqn{R^2}{R^2} (\code{glm} only): \code{"pseudo_r2_mcfadden"}
(McFadden 1974), \code{"pseudo_r2_nagelkerke"} (Nagelkerke 1991),
\code{"pseudo_r2_tjur"} (Tjur 2009; binomial only).
\item Residual scale: \code{"sigma"} (lm \eqn{\hat{\sigma}}{sigma-hat}
/ glm dispersion), \code{"rmse"}.
\item Effect size: \code{"f2"}.
\item Information criteria: \code{"AIC"}, \code{"AICc"}, \code{"BIC"},
\code{"deviance"}.
\item Change-stats for hierarchical comparison
(active under \code{nested = TRUE}; see \emph{Hierarchical
comparison} below): \code{"r2_change"}, \code{"adj_r2_change"},
\code{"f_change"}, \code{"f2_change"}, \code{"lrt_change"},
\code{"aic_change"}, \code{"aicc_change"}, \code{"bic_change"},
\code{"deviance_change"}, \code{"p_change"}.
}

Default (resolved when \code{NULL}) is class-aware: lm fits get
\code{c("nobs", "r2", "adj_r2")}; glm fits get
\code{c("nobs", "pseudo_r2_mcfadden", "pseudo_r2_nagelkerke", "AIC")};
mixed lm + glm sets union both groups (the renderer per-row
em-dashes the inappropriate cell). When \code{nested = TRUE}, the
class-aware default is extended with change tokens
(\code{c("r2_change", "f_change", "p_change")} for lm,
\code{c("lrt_change", "p_change")} for glm). The order of tokens in
\code{show_fit_stats} controls the order of the rows.
}
}

\section{Multi-model semantics}{
Pass a single fit or a \code{list()} of fits. Multi-model layout
draws a centred \strong{spanner label} above each model's
sub-columns:

\itemize{
\item \code{list("Naive" = m1, "Adjusted" = m2)} -> spanner labels
\code{"Naive"} / \code{"Adjusted"}. Partial naming (\code{list("Naive" = m1, m2)})
auto-fills missing slots as \code{"Model <position>"}.
\item \code{list(m1, m2)} (unnamed) -> if all response variables
differ, the bare DV name (from \code{formula(fit)[[2]]}) becomes
the spanner label and the redundant Outcome body row is
suppressed. If DVs match, the labels default to
\code{"Model 1, 2, ..."}.
\item \code{model_labels = c("A", "B")} overrides everything.
}

Duplicate explicit names in the list are rejected
(\code{spicy_invalid_input}) -- they would silently collide in the
internal model_id key.
}

\section{Inference and standard errors}{
\code{vcov} selects the variance-covariance estimator:

\itemize{
\item \code{"classical"} -- OLS (lm) / MLE inverse Hessian (glm).
\item \code{"HC0"} to \code{"HC5"} -- heteroskedasticity-consistent
(via \code{\link[sandwich:vcovHC]{sandwich::vcovHC()}}).
\item \code{"CR0"} to \code{"CR3"} -- cluster-robust with
Satterthwaite-corrected df (via \code{\link[clubSandwich:vcovCR]{clubSandwich::vcovCR()}}).
Requires \code{cluster}.
\item \code{"bootstrap"} -- nonparametric or cluster bootstrap
(\code{boot_n} replicates).
\item \code{"jackknife"} -- leave-one-out / leave-one-cluster-out.
}

For multi-model use, both \code{vcov} and \code{cluster} accept a single
value (recycled to all models) or a list (one per model). The
same fit can appear several times with different estimators to
compare standard errors side-by-side.

Inferential regimes (B and AME share the same regime):
\itemize{
\item \code{classical}, \verb{HC*} -> t with \code{df.residual}.
\item \code{bootstrap}, \code{jackknife} -> z asymptotic.
\item \code{CR0}-\code{CR3} -> t with \strong{Satterthwaite-corrected df} (B
via \code{\link[clubSandwich:coef_test]{clubSandwich::coef_test()}}; AME via
\code{\link[clubSandwich:linear_contrast]{clubSandwich::linear_contrast()}}; Pustejovsky & Tipton
2018). Under non-linear terms (\code{poly()}, \code{I()}, \code{log()},
\code{splines::ns()}), AME falls back to z-asymptotic with a
\code{spicy_fallback} warning.
}
\subsection{How to specify \code{cluster}}{

Three accepted forms, in order of preference:

\enumerate{
\item \strong{Formula} -- \code{cluster = ~region} (or
\code{cluster = ~region:year} for the interaction of two
variables). The variables are looked up in
\code{model.frame(fit)} first, then in the original \code{data}
argument captured by the fit. \strong{Recommended}: independent
of the dataset's name, composable for multi-way clustering,
consistent with \code{\link[sandwich:vcovCL]{sandwich::vcovCL()}} /
\code{\link[clubSandwich:vcovCR]{clubSandwich::vcovCR()}}.
\item \strong{String} -- \code{cluster = "region"}. A single column
name resolved the same way as the formula. Convenient but
cannot express interactions.
\item \strong{Vector} -- \code{cluster = df$region}. An atomic vector of
length \code{nobs(fit)}. Use this when the cluster key is
\strong{derived on the fly} (\code{cluster = interaction(df$region, df$year)},
\code{cluster = as.integer(format(df$date, "\%Y"))}), comes from
a \strong{different dataset} with matching row order, or is
otherwise not a column of the model's \code{data}.
}

Bare unquoted names (\code{cluster = region}) are \strong{not} accepted --
they would require non-standard evaluation magic that breaks
under programmatic use (function wrapping, dynamic column
choice, loops). Use \code{~region} or \code{"region"} instead.

For multi-model use, mix forms freely:
\code{cluster = list(~region, "region", df$region)}.
}
}

\section{Hierarchical (nested) model comparison}{
\code{nested = TRUE} adds \strong{per-pair change statistics as in-table
rows} (APA Table 7.13 / Stata \code{esttab} / SPSS Model Summary
convention). Each adjacent pair (M2 vs M1, M3 vs M2, ...)
contributes one column of change stats; the FIRST model column
gets em-dashes (no previous model to compare to). Validation
requires identical \code{nobs} and identical response variable
across all models.

Default change tokens auto-injected when \code{show_fit_stats} is
\code{NULL}:
\itemize{
\item All-lm: \code{c("r2_change", "f_change", "p_change")} --
APA hierarchical regression standard.
\item All-glm: \code{c("lrt_change", "p_change")} -- Hosmer &
Lemeshow Section 3.5; Long & Freese 2014 Section 3.6.
}
To customise, pass the change tokens directly to
\code{show_fit_stats}. Variance-explained change tokens on an
all-glm hierarchy raise \code{spicy_invalid_input} (the
residual-sum-of-squares partition does not apply outside the
least-squares framework -- the renderer points the user at
\code{lrt_change}).
}

\section{Standardised coefficients}{
\code{standardized} controls the method when \code{"beta"} is in
\code{show_columns}:
\itemize{
\item \code{"refit"} -- refit on z-scored data. For \code{lm} both X
and Y are z-scored (Cohen et al. 2003 gold standard); for
\code{glm} only numeric X (Long & Freese 2014 Section 4.3.4
"x-standardization").
\item \code{"posthoc"} -- post-hoc scaling. lm:
\eqn{\beta = B \times SD(X) / SD(Y)}{beta = B * SD(X) / SD(Y)};
glm: X-only \eqn{\beta = B \times SD(X)}{beta = B * SD(X)}
(Y is undefined on the link scale).
\item \code{"basic"} -- like \code{"posthoc"} but factor dummies are
scaled by their column SD.
\item \code{"smart"} -- Gelman (2008): divide binary predictors
by \code{2 * SD} instead of \code{SD}.
\item \code{"pseudo"} -- \emph{glm only}. Menard (2004, 2011)
fully-standardised \eqn{\beta = B \times SD(X) / SD(Y^*)}{beta = B * SD(X) / SD(Y*)},
with \eqn{Y^*}{Y*} the latent variable on the link scale and
\eqn{SD(Y^*) = \sqrt{Var(\hat{\eta}) + Var_{link}}}{SD(Y*) = sqrt(Var(eta-hat) + Var_link)}
(\eqn{\pi^2/3}{pi^2/3} logit, \code{1} probit,
\eqn{\pi^2/6}{pi^2/6} cloglog). Binomial families only;
non-binomial returns NA with a \code{spicy_caveat}.
\item \code{"none"} (default) -- no \eqn{\beta}{beta} computed.
}

Under interactions or transformed predictors (\code{I()}, \code{poly()},
\code{log()}, \code{splines::ns()}), a \code{spicy_caveat} warns that
standardised coefficients on such terms are subtle to interpret
(Cohen et al. 2003 Section 7.7; Aiken & West 1991). The caveat is
auto-documented in the footer.
}

\section{Multiple-comparison adjustment}{
Adjusting the p-values of all coefficients of a single
regression model is \strong{not} the standard convention. Each
coefficient tests a distinct hypothesis on a distinct
predictor -- not the situation multiple-testing procedures
were designed for (Rothman 1990; Greenland 2017; APA Manual 7
Section 6.46; Harrell \emph{Regression Modeling Strategies} Section 5.4; Gelman,
Hill & Yajima 2012). Hence the default \code{p_adjust = "none"}.

Adjustment is appropriate for: mass screening with no prior
hypothesis (typically \code{"BH"} / FDR), pre-registered
multi-endpoint confirmatory designs (typically \code{"holm"}), or
when a journal / SAP explicitly requests it.

The adjustment runs \strong{before} any \code{keep} / \code{drop} filtering,
so the family is the model's full coefficient set (intercept
and reference rows excluded), not the displayed subset --
filtering is a display choice and must not change the
inferential family.
}

\section{Output formats and broom integration}{
\code{output} selects the return type:
\itemize{
\item \code{"default"} -- a \code{spicy_regression_table}
(\code{data.frame} subclass) printed via \code{\link[=spicy_print_table]{spicy_print_table()}}.
\item \code{"data.frame"} / \code{"long"} -- raw data.frame /
long-format tibble.
\item \code{"gt"} / \code{"flextable"} / \code{"tinytable"} -- rich-format
HTML / Word / PDF tables (require the corresponding
Suggests package).
\item \code{"excel"} -- writes to \code{excel_path} via
\code{\link[openxlsx2:write_xlsx]{openxlsx2::write_xlsx()}}.
\item \code{"word"} -- writes to \code{word_path} via
\code{flextable::save_as_docx()}.
\item \code{"clipboard"} -- copies to the system clipboard via
\code{\link[clipr:write_clip]{clipr::write_clip()}}.
}

\code{\link[broom:tidy]{broom::tidy()}} returns a long tibble with one row per
\verb{(model_id, term, estimate_type)} and broom-canonical column
names (\code{estimate}, \code{std.error}, \code{conf.low}, \code{conf.high},
\code{statistic}, \code{p.value}). \code{\link[broom:glance]{broom::glance()}} returns one row per
model with the model-level statistics; \code{df.residual} is kept
numeric so cluster-robust Satterthwaite df is preserved.
}

\section{Weights}{
No \code{weights} argument: weights are a property of the fit
(extracted via \code{\link[stats:weights]{stats::weights()}}). Pass them when fitting:
\code{lm(y ~ x, data = df, weights = w)}. All downstream
computations (vcov, AME, standardisation, \code{weighted_nobs})
extract them automatically.
}

\section{Internationalisation}{
Output is in English. Override user-facing strings via
\code{reference_label}, \code{model_labels}, \code{outcome_labels}, and
\code{labels}. The title and footer are post-processable via
\code{attr(result, "title")} and \code{attr(result, "note")}.
}

\section{Classed conditions}{

Every error and warning emitted by \code{table_regression()} carries
a classed condition for programmatic dispatch via \code{\link[=tryCatch]{tryCatch()}}
or \code{\link[=withCallingHandlers]{withCallingHandlers()}}. Errors inherit from \code{spicy_error}
(root); warnings from \code{spicy_warning}. Specific leaves used by
this function include \code{spicy_invalid_input},
\code{spicy_invalid_data}, \code{spicy_unsupported},
\code{spicy_missing_pkg}, \code{spicy_missing_column},
\code{spicy_ignored_arg}, \code{spicy_caveat}, \code{spicy_fallback}. See
\code{\link[=spicy-package]{spicy}} for the full taxonomy.
}

\examples{
fit <- lm(wellbeing_score ~ age + sex + education,
          data = sochealth)

# Default APA layout (single model)
table_regression(fit)

# Standardised coefficients (beta) alongside B
table_regression(fit, standardized = "refit")

# Custom column set: AME alongside its own p-value
table_regression(
  fit,
  show_columns = c("b", "p", "ame", "ame_ci", "ame_p")
)

# Group-token shortcuts: "all_b" + "all_ame" expand to the
# full B / AME column families.
table_regression(fit, show_columns = c("all_b", "all_ame"))

# Cluster-robust SE (CR2 with Satterthwaite df). Three accepted
# forms for `cluster`; the formula is preferred.
table_regression(fit, vcov = "CR2", cluster = ~region)
table_regression(fit, vcov = "CR2", cluster = "region")
table_regression(fit, vcov = "CR2", cluster = sochealth$region)

# Multi-way clustering via formula interaction
table_regression(fit, vcov = "CR2", cluster = ~region:age_group)

# Hierarchical (nested) regression: change-stat rows appear
# below R-squared / Adj.R-squared.
m1 <- lm(wellbeing_score ~ age,                       data = sochealth)
m2 <- lm(wellbeing_score ~ age + sex,                 data = sochealth)
m3 <- lm(wellbeing_score ~ age + sex + education,     data = sochealth)
table_regression(
  list("Step 1" = m1, "Step 2" = m2, "Step 3" = m3),
  nested = TRUE
)

# Side-by-side SE comparison (same fit, three vcovs)
table_regression(
  list("Classical" = fit, "HC3" = fit, "CR2" = fit),
  vcov    = list("classical", "HC3", "CR2"),
  cluster = list(NULL, NULL, ~region)
)

# Tidy long format for downstream pipelines
broom::tidy(table_regression(fit))

\dontrun{
# Rich-format outputs (require optional Suggests packages)
table_regression(fit, output = "gt")
table_regression(fit, output = "flextable")
table_regression(fit, output = "tinytable")

# File outputs
table_regression(fit, output = "excel",
                 excel_path = tempfile(fileext = ".xlsx"))
table_regression(fit, output = "word",
                 word_path  = tempfile(fileext = ".docx"))

# System clipboard (interactive use)
table_regression(fit, output = "clipboard")
}

}
\references{
APA Manual 7 (American Psychological Association, 2020),
Tables 7.13-7.15.

Aiken, L.S. & West, S.G. (1991). \emph{Multiple regression: Testing
and interpreting interactions}.

Cohen, J., Cohen, P., West, S.G., & Aiken, L.S. (2003).
\emph{Applied multiple regression / correlation analysis for the
behavioral sciences} (3rd ed.). Lawrence Erlbaum.

Pustejovsky, J.E. & Tipton, E. (2018). Small-sample methods for
cluster-robust variance estimation and hypothesis testing in
fixed effects models. \emph{Journal of Business & Economic
Statistics}, 36(4), 672-683.

Wasserstein, R.L., Schirm, A.L., & Lazar, N.A. (2019). Moving
to a world beyond "p < 0.05". \emph{The American Statistician},
73(sup1), 1-19.
}
\seealso{
Other regression-table functions:
\code{\link[=table_continuous_lm]{table_continuous_lm()}} for one-predictor-by-many-outcomes
descriptive tables.
Other spicy table functions:
\code{\link[=freq]{freq()}}, \code{\link[=cross_tab]{cross_tab()}}, \code{\link[=table_categorical]{table_categorical()}},
\code{\link[=table_continuous]{table_continuous()}}.
Underlying machinery:
\code{\link[=spicy_print_table]{spicy_print_table()}} for ASCII rendering;
\code{\link[=build_ascii_table]{build_ascii_table()}} for the low-level renderer.
Inferential infrastructure (internal):
\code{compute_lm_vcov()}, \code{compute_lm_coef_inference()},
\code{compute_lm_wald_test()}.
broom integration:
\code{\link[broom:tidy]{broom::tidy()}}, \code{\link[broom:glance]{broom::glance()}}.
}
