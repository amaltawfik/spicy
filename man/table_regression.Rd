% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_regression.R
\name{table_regression}
\alias{table_regression}
\title{Regression coefficient summary table}
\usage{
table_regression(
  models,
  vcov = "classical",
  cluster = NULL,
  ci_level = 0.95,
  boot_n = 1000L,
  standardized = c("none", "refit", "posthoc", "basic", "smart"),
  exponentiate = FALSE,
  p_adjust = "none",
  show_columns = c("B", "SE", "CI", "p"),
  keep = NULL,
  drop = NULL,
  show_intercept = TRUE,
  intercept_position = c("first", "last"),
  group_factor_levels = TRUE,
  reference_style = c("row", "annotation"),
  reference_label = "(ref.)",
  show_fit_stats = NULL,
  model_labels = NULL,
  outcome_labels = NULL,
  stars = FALSE,
  nested = FALSE,
  nested_stats = NULL,
  digits = 2L,
  p_digits = 3L,
  effect_size_digits = 2L,
  fit_digits = 2L,
  ic_digits = 1L,
  decimal_mark = ".",
  align = c("decimal", "center", "right", "auto"),
  labels = NULL,
  output = c("default", "data.frame", "long", "gt", "flextable", "tinytable", "excel",
    "clipboard", "word"),
  excel_path = NULL,
  excel_sheet = "Regression",
  clipboard_delim = "\\t",
  word_path = NULL
)
}
\arguments{
\item{models}{An \code{lm} fitted model \strong{or} a list of \code{lm} fits
(named or unnamed). Single fits are auto-promoted to a
one-element list internally. \code{glm}, \code{merMod}, and other
classes are rejected with an actionable error in Phase 1.
Raw data + formula is \strong{not} accepted (fit-only API,
matching \code{broom}, \code{modelsummary}, \code{gtsummary}, and
\code{marginaleffects}).}

\item{vcov}{Variance-covariance estimator. A single string
(one of \code{"classical"}, \code{"HC0"} to \code{"HC5"}, \code{"CR0"} to
\code{"CR3"}, \code{"bootstrap"}, \code{"jackknife"}) recycled to all models,
\strong{or} a list of strings (one per model) for the pedagogical
side-by-side SE-comparison use case. Default \code{"classical"}.}

\item{cluster}{Cluster identifier for cluster-robust variance
(\code{vcov} in \code{"CR0"} to \code{"CR3"} or \code{"bootstrap"} /
\code{"jackknife"} cluster forms). Either an unquoted column name,
a single string column name, an atomic vector of length
\code{nobs(model)}, or a list (one per model). Default \code{NULL}
(no clustering).}

\item{ci_level}{Confidence level for all reported CIs (B, β,
AME, partial effect sizes). Default \code{0.95}.}

\item{boot_n}{Number of bootstrap replicates when
\code{vcov = "bootstrap"}. Single positive integer. Default
\code{1000L}.}

\item{standardized}{Standardisation method for the \code{"beta"}
column. One of \code{"none"} (default), \code{"refit"}, \code{"posthoc"},
\code{"basic"}, \code{"smart"}. See the \emph{Standardised coefficients}
section.}

\item{exponentiate}{Logical. When \code{TRUE} and the model is a
\code{glm()} with a non-identity link, the displayed coefficient
(\code{B}), confidence interval bounds, and standard error are
transformed to the response scale via \code{exp()}. The column
header is rebranded per family / link: \code{OR} for binomial(logit),
\code{IRR} for poisson(log), \code{HR} for binomial(cloglog), \code{RR} for
binomial(log), \code{MR} for Gamma(log), and the generic \code{exp(B)}
otherwise. The standard error follows the delta-method
approximation \verb{SE_OR = OR × SE_log-odds} (Stata \verb{logit, or},
\code{parameters::model_parameters()}); the test statistic and the
p-value remain on the link scale (both are invariant under
monotone transformation). Default \code{FALSE} (parameters /
modelsummary convention — explicit opt-in over silent transform).
Has no effect on \code{lm()} fits or on identity-link glm; emits a
\code{spicy_ignored_arg} warning in those cases.}

\item{p_adjust}{Multiple-comparison adjustment method applied to
the family of estimated coefficient p-values within each model
(intercept and reference rows excluded by convention). One of
\code{"none"} (default — no adjustment), \code{"holm"}, \code{"hochberg"},
\code{"hommel"}, \code{"bonferroni"}, \code{"BH"} / \code{"fdr"}, or \code{"BY"}. The
adjustment is delegated to \code{\link[stats:p.adjust]{stats::p.adjust()}} and applied
per-model and per \code{estimate_type} (so B and AME p-values are
adjusted independently within their own families). When active,
a footer note documents the method and the family size. The
adjustment runs \strong{before} any \code{keep} / \code{drop} filtering so the
family is the model's full coefficient set, not just the
displayed subset (modelsummary convention).

\strong{Methodological note.} Adjusting the p-values of all
coefficients of a single regression model is \emph{not} the standard
convention in social-science / clinical reporting. Each
coefficient tests a scientifically distinct hypothesis on a
distinct predictor, which is not the situation multiple-testing
procedures were designed for (Rothman 1990; Greenland 2017).
The default \code{"none"} reflects this: APA Manual 7 §6.46,
Harrell (\emph{Regression Modeling Strategies}, §5.4), and Gelman,
Hill & Yajima (2012) all recommend reporting unadjusted
p-values for the coefficients of a pre-specified regression
model. Cases where adjustment IS appropriate include mass
screening with many candidate predictors and no prior
hypothesis (typically \code{"BH"} / FDR), pre-registered
multi-endpoint confirmatory designs (typically \code{"holm"}), or
when a journal / SAP explicitly requests it. spicy exposes the
argument under the same "transparency over rejection" rule
used for \code{standardized}: the tool is available, the
methodological choice is yours.}

\item{show_columns}{Character vector of tokens controlling
which per-coefficient columns to display, \strong{and} in which
order. See the \emph{Vocabulary tokens} section. Default
\code{c("B", "SE", "CI", "p")}.}

\item{keep}{Character vector of regular expressions; when
supplied, only coefficient rows whose term name matches at
least one of the patterns are kept. Useful when a model has
many control variables and you want to display only the focal
predictors. Mutually exclusive with \code{drop}. Matching is
applied to the coefficient name as it appears in
\code{\link[stats:coef]{stats::coef()}} (e.g., \code{"wt"}, \code{"cyl6"}, \code{"factor(cyl)8"}).
\code{p_adjust} is computed before the filter, so adjusted p-values
reflect the model's full coefficient family. Default \code{NULL}
(no filter).}

\item{drop}{Character vector of regular expressions; when
supplied, coefficient rows whose term name matches any of the
patterns are removed. Mutually exclusive with \code{keep}. Useful
for hiding control variables from a publication-ready table.
Same matching semantics as \code{keep}. Default \code{NULL} (no filter).}

\item{show_intercept}{Whether to display the intercept row.
Default \code{TRUE} (APA convention). Hide via \code{FALSE}.}

\item{intercept_position}{Where to place the intercept when
shown. \code{"first"} (default, APA) or \code{"last"} (Stata-style,
intercept just above the fit-stats footer). Ignored when
\code{show_intercept = FALSE} (with \code{spicy_ignored_arg} warning).}

\item{group_factor_levels}{Whether to render factor predictors
as a header row + indented contrast levels. \code{TRUE} (default,
APA / gtsummary style) or \code{FALSE} (flat \code{factor: level} rows).}

\item{reference_style}{Rendering of factor reference levels.
\code{"row"} (default) shows an explicit row \code{Lower (ref.)} with
em-dashes in all stat columns (gtsummary / clinical style).
\code{"annotation"} puts \verb{[ref: Lower]} in the factor header
(compact mode).}

\item{reference_label}{String used to mark the reference level
in \code{reference_style = "row"} mode. Default \code{"(ref.)"}.
Customise to \code{"(reference)"} or \code{"(réf.)"} etc.}

\item{show_fit_stats}{Character vector of tokens for the
model-level fit-stats footer. See \emph{Vocabulary tokens}.
Default \code{c("nobs", "r2", "adj_r2")}.}

\item{model_labels}{Column / model labels for multi-model
tables. \code{NULL} (default) uses smart auto-generation: hidden
for a single model, \code{"Model 1, 2, 3"} or \code{names(list)} for
multiple models. A character vector of length \code{length(models)}
forces explicit per-column labels.}

\item{outcome_labels}{Per-model response-variable row.
\code{NULL} (default) uses smart auto: row hidden when all DVs
identical, row shown when DVs differ. Auto-derived from
\code{attr(data[[dv]], "label")} if present, else from
\code{formula(fit)[[2]]}. A character vector of length
\code{length(models)} forces explicit labels. \code{FALSE} suppresses
the row entirely.}

\item{stars}{Significance asterisk display.
\code{FALSE} (default, APA-aligned) — no stars; p-values reported
as numbers only. APA 7 §6.46 explicitly discourages stars.
\code{TRUE} — APA-conventional cutoffs
\code{c("*" = 0.05, "**" = 0.01, "***" = 0.001)}.
A named numeric vector — custom thresholds and symbols (e.g.
\code{c("†" = 0.10, "*" = 0.05, "**" = 0.01, "***" = 0.001)}).}

\item{nested}{Whether to add a hierarchical-comparison footer
block (each model vs the previous via \code{\link[stats:anova]{stats::anova()}} or
equivalent). \code{FALSE} (default) — pure side-by-side display
without comparison statistics. \code{TRUE} — adds the comparison
footer; requires identical \code{nobs} and identical response
variable across all models.}

\item{nested_stats}{Tokens controlling which comparison
statistics to display when \code{nested = TRUE}. \code{NULL} (default)
selects the class-aware default (\code{c("r2_change", "F", "p")}
for \code{lm}). See \emph{Vocabulary tokens}.}

\item{digits}{Number of decimal places for general numeric
tokens (\code{B}, \code{beta}, \code{SE}, \code{CI}, \code{t}, \code{F}, \code{LRT}, \code{deviance},
\code{deviance_change}, \code{AME}, \code{AME_SE}, \code{weighted_nobs}).
Default \code{2L}.}

\item{p_digits}{Decimal places for p-values (\code{p}, \code{AME_p}, and
\code{p} inside \code{nested_stats}). APA-strict: leading zero stripped,
\verb{<.001} (or \verb{<.0001} etc. depending on \code{p_digits}) for small
values. Default \code{3L}.}

\item{effect_size_digits}{Decimals for per-coefficient effect
sizes (\code{partial_f2}, \code{partial_eta2}, \code{partial_omega2}).
Default \code{2L}.}

\item{fit_digits}{Decimals for variance-explained / model-level
effect-size fit stats (\code{r2}, \code{adj_r2}, \code{r2_change},
\code{adj_r2_change}, \code{omega2}, \code{f2}, \code{f2_change}, \code{sigma}, \code{rmse}).
Default \code{2L}.}

\item{ic_digits}{Decimals for information criteria (\code{AIC},
\code{AICc}, \code{BIC} in both \code{show_fit_stats} and \code{nested_stats},
plus their Δ-form). Default \code{1L}.}

\item{decimal_mark}{Decimal mark used in numeric display.
\code{"."} (default) or \code{","} (European convention). When
\code{","} is used, the CI bracket separator switches to \code{"; "}
automatically to avoid \code{"0,18 [0,07, 0,30]"} ambiguity.}

\item{align}{Numeric column alignment.
\code{"decimal"} (default) — pre-pad cells so decimal marks line
up vertically (publication-style).
\code{"center"}, \code{"right"}, or \code{"auto"} for legacy per-column
alignment.}

\item{labels}{Named character vector overriding per-coefficient
row labels. Names are coefficient term names (from
\code{\link[stats:terms]{stats::terms()}}); values are the displayed labels. E.g.
\code{c("age" = "Age (years)", "sexM" = "Male (vs Female)")}.
Default \code{NULL} (use raw term names).}

\item{output}{Output type. \code{"default"} (a printable
\code{spicy_regression_table}); \code{"data.frame"} / \code{"long"} (raw
data); \code{"gt"} / \code{"flextable"} / \code{"tinytable"} (rich-format
tables); \code{"excel"} (writes to \code{excel_path}); \code{"clipboard"}
(copies to system clipboard); \code{"word"} (writes flextable to
\code{word_path}).}

\item{excel_path}{File path for \code{output = "excel"}. Default
\code{NULL} (required when \code{output = "excel"}).}

\item{excel_sheet}{Sheet name when writing to Excel. Default
\code{"Regression"}.}

\item{clipboard_delim}{Field delimiter for
\code{output = "clipboard"}. Default \code{"\\t"} (tab-separated, pastes
cleanly into Excel / Google Sheets).}

\item{word_path}{File path for \code{output = "word"}. Default
\code{NULL} (required when \code{output = "word"}).}
}
\value{
A \code{spicy_regression_table} object (a \code{data.frame}
subclass with classes \code{c("spicy_regression_table", "spicy_table", "data.frame")}) when \code{output = "default"}.
The result carries rendering attributes (\code{title}, \code{note},
\code{align}, \code{padding}) and provenance attributes (\code{outcome},
\code{model_ids}) consumed by the print method and the broom
methods. For other \code{output} values, returns the
format-specific object (\code{gt_tbl}, \code{flextable}, \code{tinytable},
\code{data.frame}, \code{tbl_df}, or \code{invisible(x)} for side-effect
outputs).
}
\description{
Build a publication-ready summary table from one or more fitted
regression models. Designed for \strong{APA-strict} reporting in
psychology, public health, sociology, and biostatistics, with
optional opt-in conventions for econometrics and clinical trials.

Phase 1 supports \code{lm} only. Logistic / probit / Poisson regression
(\code{glm}) is planned for spicy 0.15.0; mixed-effects models
(\code{lmerMod}, \code{glmerMod}) for spicy 0.16+.
}
\section{Vocabulary tokens}{
Three vector arguments — \code{show_columns}, \code{show_fit_stats}, and
\code{nested_stats} — accept named tokens that select \strong{what} to
display and in \strong{what order}. Tokens use snake_case (lowercase),
with capitalisation preserved only for canonical statistical
abbreviations (\code{AIC}, \code{BIC}, \code{F}, \code{LRT}, \code{B}, \code{SE}, \code{CI}, \code{AME}).
\subsection{\code{show_columns} — per-coefficient columns (in main table)}{

Estimate-related: \code{B}, \code{beta}, \code{SE}, \code{CI}, \code{t}, \code{p}.
Effect sizes: \code{partial_f2}, \code{partial_eta2}, \code{partial_omega2}
(compact \code{value [CI]} rendering).
Marginal effects: \code{AME}, \code{AME_p}, \code{AME_SE} (compact for \code{AME}).

Default: \code{c("B", "SE", "CI", "p")}.
If \code{standardized != "none"} and \code{"beta"} is not in \code{show_columns},
it is auto-injected after \code{B}. Asking for \code{"beta"} while
\code{standardized = "none"} raises \code{spicy_invalid_input}.
}

\subsection{\code{show_fit_stats} — model-level statistics (in footer)}{

Counts: \code{nobs}, \code{weighted_nobs}.
Variance explained: \code{r2}, \code{adj_r2}, \code{omega2}.
Residual scale: \code{sigma}, \code{rmse}.
Effect size: \code{f2}.
Information criteria: \code{AIC}, \code{AICc}, \code{BIC}, \code{deviance}.

Default: \code{c("nobs", "r2", "adj_r2")}.
}

\subsection{\code{nested_stats} — hierarchical comparison footer}{

Activates when \code{nested = TRUE}. Tokens: \code{r2_change},
\code{adj_r2_change}, \code{F}, \code{f2_change}, \code{LRT}, \code{AIC}, \code{AICc}, \code{BIC},
\code{deviance_change}, \code{p}.

Default for \code{lm} (\code{nested_stats = NULL}):
\code{c("r2_change", "F", "p")} — the APA hierarchical regression
standard.
}
}

\section{Multi-model semantics}{
Pass a single \code{lm} fit or a \code{list()} of fits. A named list
(\code{list("Naive" = m1, "Adjusted" = m2)}) provides column headers
automatically; an unnamed list defaults to \code{"Model 1, 2, ..."}.
\code{model_labels} overrides both with explicit per-column labels.

For models with \strong{different response variables}, the DV name is
shown automatically in a header row (controllable via
\code{outcome_labels}). For models sharing the same DV, the DV is
mentioned in the table title only.
}

\section{Inference and standard errors}{
\code{vcov} selects the variance-covariance estimator: \code{"classical"}
(OLS), \code{"HC0"} to \code{"HC5"} (heteroskedasticity-consistent via
\code{\link[sandwich:vcovHC]{sandwich::vcovHC()}}), \code{"CR0"} to \code{"CR3"} (cluster-robust via
\code{\link[clubSandwich:vcovCR]{clubSandwich::vcovCR()}} with Satterthwaite-corrected df),
\code{"bootstrap"} (nonparametric or cluster bootstrap), or
\code{"jackknife"} (leave-one-out / leave-one-cluster-out).

For multi-model use, both \code{vcov} and \code{cluster} accept either a
single value (recycled to all models) \strong{or} a list (one per
model). The pedagogical use case \code{list(fit, fit, fit)} with
\code{vcov = list("classical", "HC3", "CR2")} enables side-by-side
SE-comparison in a single table.

Inferential regimes (B and AME share the same regime by design):
\itemize{
\item \code{classical}, \verb{HC*} → t with \code{df.residual}
\item \code{bootstrap}, \code{jackknife} → z asymptotic
\item \verb{CR0–CR3} → t with \strong{Satterthwaite-corrected df} (B via
\code{\link[clubSandwich:coef_test]{clubSandwich::coef_test()}}; AME via
\code{\link[clubSandwich:linear_contrast]{clubSandwich::linear_contrast()}} — see
Pustejovsky & Tipton 2018). spicy is the first R package to
offer AME-Satterthwaite under cluster-robust variance for
\code{lm}. For models with non-linear formulas (\code{poly()}, \code{I()},
\code{log()}, \code{splines::ns()}), AME inference falls back to
z-asymptotic with a \code{spicy_fallback} warning.
}
}

\section{Standardised coefficients}{
\code{standardized} controls the standardisation method when \code{"beta"}
is in \code{show_columns}:
\itemize{
\item \code{"refit"} — refit the model on z-scored predictors and
outcome (gold standard, Cohen et al. 2003).
\item \code{"posthoc"} — post-hoc scaling \verb{β = B × SD(X) / SD(Y)}.
\item \code{"basic"} — like posthoc but factor dummies stay at 0/1.
\item \code{"smart"} — Gelman (2008) recommendation: divide by \verb{2 × SD}
for binary predictors.
\item \code{"none"} (default) — no β computed.
}

For models with interactions or transformed predictors (\code{I()},
\code{poly()}, \code{log()}, \code{splines::ns()}), a \code{spicy_caveat} warning is
emitted reminding that standardised coefficients on such terms
should be interpreted with care (Cohen et al. 2003 §7.7;
Aiken & West 1991). The footer auto-documents the caveat.
}

\section{Hierarchical (nested) model comparison}{
Set \code{nested = TRUE} to add a footer block comparing each model
to the previous one. spicy validates that all models share
identical \code{nobs} AND identical response variable, raising
\code{spicy_invalid_input} otherwise (with a remediation snippet).

Class-aware default \code{nested_stats} for \code{lm}:
\code{c("r2_change", "F", "p")}. Customise via the \code{nested_stats}
argument with any combination of supported tokens.
}

\section{Output formats and broom integration}{
\code{output} selects the return type:
\itemize{
\item \code{"default"} — a \code{spicy_regression_table} object (a
\code{data.frame} subclass) printed via \code{\link[=spicy_print_table]{spicy_print_table()}}.
\item \code{"data.frame"} / \code{"long"} — raw data.frame / long-format
tibble (the underlying analytic representation).
\item \code{"gt"} / \code{"flextable"} / \code{"tinytable"} — rich-format HTML /
Word / PDF tables (requires the corresponding package).
\item \code{"excel"} — writes to \code{excel_path} via
\code{\link[openxlsx2:write_xlsx]{openxlsx2::write_xlsx()}} (returns \code{invisible(x)}).
\item \code{"clipboard"} — copies to system clipboard via
\code{\link[clipr:write_clip]{clipr::write_clip()}} (returns \code{invisible(x)}).
}

\code{\link[broom:tidy]{broom::tidy()}} returns a long tibble with one row per
\verb{(model_id, term, estimate_type)} and broom-canonical column
names (\code{estimate}, \code{std.error}, \code{conf.low}, \code{conf.high},
\code{statistic}, \code{p.value}). \code{\link[broom:glance]{broom::glance()}} returns one row per
model with the model-level statistics. \code{df.residual} is kept
numeric (not integer) so cluster-robust Satterthwaite df is
preserved verbatim.
}

\section{Output encoding}{
\code{table_regression()} produces UTF-8 output with Unicode
box-drawing for table layout and Greek / mathematical symbols
(β, ω², χ², f², Δ, †, em-dash). A UTF-8 capable terminal is
required (default in RStudio, R ≥ 4.0 on Windows 10+, macOS,
modern Linux).
}

\section{Internationalisation}{
Output is in English. To localise visible text:
\itemize{
\item Use \code{reference_label}, \code{model_labels}, \code{outcome_labels},
and \code{labels} to override user-customisable strings.
\item For the title and footer, post-process the result via
\code{attr(result, "title") <- "..."} and
\code{attr(result, "note") <- "..."}.
}
}

\section{Weights}{
There is \strong{no} \code{weights} argument. Weights are a property of
the fitted model (extracted via \code{\link[stats:weights]{stats::weights()}}); pass them
when fitting:
\preformatted{
fit <- lm(y ~ x, data = df, weights = w)
table_regression(fit)
}
All downstream computations (vcov, AME, standardisation,
\code{weighted_nobs}) extract the weights automatically.
}

\section{Classed conditions}{

Every error and warning emitted by \code{table_regression()} carries
a classed condition for programmatic dispatch via \code{\link[=tryCatch]{tryCatch()}}
or \code{\link[=withCallingHandlers]{withCallingHandlers()}}. Errors inherit from \code{spicy_error}
(root); warnings from \code{spicy_warning}. Specific leaves used by
this function include \code{spicy_invalid_input},
\code{spicy_invalid_data}, \code{spicy_unsupported},
\code{spicy_missing_pkg}, \code{spicy_missing_column},
\code{spicy_ignored_arg}, \code{spicy_caveat}, \code{spicy_singular},
\code{spicy_fallback}. See \code{\link[=spicy-package]{spicy}} for the
full taxonomy.
}

\examples{
\dontrun{
library(spicy)

# Single model, default APA layout
fit <- lm(wellbeing_score ~ age + sex + education,
          data = sochealth)
table_regression(fit)

# Hierarchical regression: nested = TRUE adds the comparison
# footer (ΔR², partial F, p)
m1 <- lm(wellbeing_score ~ age, data = sochealth)
m2 <- lm(wellbeing_score ~ age + sex, data = sochealth)
m3 <- lm(wellbeing_score ~ age + sex + education,
         data = sochealth)
table_regression(
  list("Step 1" = m1, "Step 2" = m2, "Step 3" = m3),
  nested = TRUE
)

# Cluster-robust SEs with Satterthwaite df
fit_cl <- lm(wellbeing_score ~ age + sex,
             data = sochealth, weights = weight)
table_regression(
  fit_cl,
  vcov = "CR2",
  cluster = clinic_id
)

# Standardised coefficients (β) alongside B
table_regression(fit, standardized = "refit")

# Custom column set: B, partial f², AME with CI, p
table_regression(
  fit,
  show_columns = c("B", "partial_f2", "AME", "p")
)

# Pedagogical side-by-side SE comparison (same fit, three vcovs)
table_regression(
  list("Classical" = fit, "HC3" = fit, "CR2" = fit),
  vcov = list("classical", "HC3", "CR2"),
  cluster = list(NULL, NULL, sochealth$clinic_id)
)

# Output to gt
table_regression(fit, output = "gt")

# Tidy long format for downstream pipelines
broom::tidy(table_regression(fit))
}

}
\references{
APA Manual 7 (American Psychological Association, 2020),
Tables 7.13–7.15.

Aiken, L.S. & West, S.G. (1991). \emph{Multiple regression: Testing
and interpreting interactions}.

Cohen, J., Cohen, P., West, S.G., & Aiken, L.S. (2003).
\emph{Applied multiple regression / correlation analysis for the
behavioral sciences} (3rd ed.). Lawrence Erlbaum.

Pustejovsky, J.E. & Tipton, E. (2018). Small-sample methods for
cluster-robust variance estimation and hypothesis testing in
fixed effects models. \emph{Journal of Business & Economic
Statistics}, 36(4), 672–683.

Wasserstein, R.L., Schirm, A.L., & Lazar, N.A. (2019). Moving
to a world beyond "p < 0.05". \emph{The American Statistician},
73(sup1), 1–19.
}
\seealso{
Other regression-table functions:
\code{\link[=table_continuous_lm]{table_continuous_lm()}} for one-predictor-by-many-outcomes
descriptive tables.
Other spicy table functions:
\code{\link[=freq]{freq()}}, \code{\link[=cross_tab]{cross_tab()}}, \code{\link[=table_categorical]{table_categorical()}},
\code{\link[=table_continuous]{table_continuous()}}.
Underlying machinery:
\code{\link[=spicy_print_table]{spicy_print_table()}} for ASCII rendering;
\code{\link[=build_ascii_table]{build_ascii_table()}} for the low-level renderer.
Inferential infrastructure (internal):
\code{compute_lm_vcov()}, \code{compute_lm_coef_inference()},
\code{compute_lm_wald_test()}.
broom integration:
\code{\link[broom:tidy]{broom::tidy()}}, \code{\link[broom:glance]{broom::glance()}}.
}
