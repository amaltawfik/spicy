% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regression_broom.R
\name{tidy.spicy_regression_table}
\alias{tidy.spicy_regression_table}
\alias{glance.spicy_regression_table}
\title{Tidy / glance methods for \code{spicy_regression_table}}
\usage{
\method{tidy}{spicy_regression_table}(x, ...)

\method{glance}{spicy_regression_table}(x, ...)
}
\arguments{
\item{x}{A \code{spicy_regression_table} returned by
\code{\link[=table_regression]{table_regression()}}.}

\item{...}{Currently ignored. Present for compatibility with the
\code{\link[broom:tidy]{broom::tidy()}} / \code{\link[broom:glance]{broom::glance()}} generics.}
}
\value{
A \code{tbl_df} (when \code{tibble} is installed) or a plain
\code{data.frame}.
}
\description{
Standard \code{\link[broom:tidy]{broom::tidy()}} / \code{\link[broom:glance]{broom::glance()}} interfaces for an
object returned by \code{\link[=table_regression]{table_regression()}}. They re-shape the
underlying long-format data into the two canonical broom views
so the table can be consumed by \code{gtsummary}, \code{modelsummary},
\code{parameters}, and any tidyverse-stats pipeline.
}
\details{
\code{tidy()} returns one row per \verb{(model_id, term, estimate_type)}
triplet, with \code{estimate_type} in
\code{c("B", "beta", "AME", "partial_f2", "partial_eta2", "partial_omega2")}.
Reference-row placeholders (factor reference levels) and
singular coefficients (NA estimates) are dropped. Columns:
\verb{model_id, outcome, term, estimate_type, estimate, std.error, conf.low, conf.high, statistic, df, p.value, test_type, is_intercept, factor_term, factor_level}.

\code{glance()} returns one row per \verb{(model_id, outcome)} with
model-level statistics. Columns: \verb{model_id, outcome, nobs, weighted_nobs, r.squared, adj.r.squared, omega2, sigma, rmse, f2, AIC, AICc, BIC, deviance, df.residual} (numeric --
Satterthwaite-safe).
}
\seealso{
\code{\link[=as.data.frame.spicy_regression_table]{as.data.frame.spicy_regression_table()}} for the wide
raw view.
}
\keyword{internal}
